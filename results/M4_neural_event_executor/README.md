# M4 Neural Event Executor Results

- `summary.json` records the current neural structured-event checkpoint.
- The current learned branch is exact on the exported train and held-out slices.
- The current learned branch is still opcode-conditioned and should be read as a
  narrow `C2c` checkpoint, not as a token-level executor result.
