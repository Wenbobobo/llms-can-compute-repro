# Claims Matrix

Current live routing is controlled by `H63/P50/P51/P52/F38` plus the current
stage driver, with preserved prior packets
`H62/H61/H60/H59/H58/F37/F36/F35/F32/P49/P48/P47/P46/P45/F34/P44/P43/P42/P41/H56/H57/H54/H52/H50`
still visible underneath the active stack. Many rows below are historical and may use
"current" or similar language relative to the packet that landed them. When
historical wording conflicts with live routing, trust current control surfaces
first.

| Claim | Minimal technical interpretation | Experiment | Status |
| --- | --- | --- | --- |
| A1 | Computation can be encoded as an append-only trace | Reference interpreter plus replay engine agree on final state | validated for current toy programs |
| A2 | Useful state can be recovered from bounded trace-local semantics | Trace events expose explicit pops, pushes, branches, next-PC transitions, and latest memory writes | validated for current stack + bounded RAM programs |
| B1 | Exact 2D hard-max retrieval can be implemented exactly | Brute-force reference path over 2D keys/values/queries | validated on current tests |
| B2 | A specialized cache can answer the same retrieval sublinearly | `HullKVCache` query agrees with brute force and benchmarks better than linear scan | validated for current benchmark sizes |
| B3 | Tie handling is part of semantics, not a detail | Exact averaging over equal maximizers, including degeneracies | validated on current tie tests |
| B4 | The minimal Origin-core retrieval primitives remain exact on real extracted traces | `R34_origin_retrieval_primitive_contract_gate` over latest-write, stack top/depth, call-return target, and tie semantics | validated on the current narrow primitive suite |
| B5 | The first semantic-boundary retrieval contract preserves exact value and maximizer-row identity on fixed append-only memory tasks | `R42_origin_append_only_memory_retrieval_contract_gate` over latest-write-by-address, stack-slot depth, duplicate/tie, and range-sweep rows | validated on six fixed contract tasks only; does not yet validate bounded-memory VM execution, coequal model mainline execution, or useful-case kernels |
| C1 | These primitives can support a small exact executor | Tiny stack-machine interpreter, replay engine, and bounded RAM latest-write semantics | validated on the current narrow suite |
| C1a | Latest-write memory reads can be rewritten as exact 2D hard-max causal retrieval | Linear scan and `HullKVCache` agree on extracted memory-read events from real traces | validated for immediate and dynamic bounded RAM examples |
| C1b | Stack-slot reads can be rewritten as exact 2D hard-max causal retrieval | Linear scan and `HullKVCache` agree on extracted stack-slot read events from real traces | validated for current stack-machine examples |
| C1c | A narrow trainable scorer can recover latest-write stack retrieval with the same inductive bias | Fit a two-parameter scorer on short countdown stack traces, then evaluate on longer countdown traces and a dynamic-memory stack trace | validated for current toy traces |
| C1d | An online executor can recover value state from append-only latest-write history during rollout | Free-running exact linear and accelerated executors reproduce the reference trace on current stack, branch, and bounded-RAM programs | validated for current toy programs |
| C1e | Call/return targets can be expressed as append-only latest-write frame retrieval | `R34` call-frame primitive audit over the nested `call_chain` trace | validated for the current narrow call chain |
| C1f | A retrieval-backed executor can keep call/return state without a hidden Python-only call-stack side channel | `R35_origin_append_only_stack_vm_execution_gate` pointer-like exact rollout on the current stack-VM bundle | validated for the current narrow execution bundle |
| C2 | The executor survives longer free-running rollout on a mixed narrow stack-VM bundle | `R35` exact trace/final-state evaluation on straight-line, loop, indirect-memory, control-flow, mixed-surface, and call/return cases | validated for the current narrow bundle |
| C2a | A narrow learned retrieval rule can stay exact in free-running rollout on current stack-family programs | Fit on short countdowns, then run the scorer online in held-out countdown, branch, and current bounded-RAM traces | validated for current toy programs |
| C2b | A trace-induced structured executor can generate event decisions online without hand-coded opcode semantics | Fit per-opcode transition rules from reference traces, then run them online with latest-write retrieval on held-out countdown, branch, and indirect-memory programs | validated for current toy programs |
| C2c | A neural causal executor can generate event decisions online without exact fallback | Train an opcode-conditioned neural decoder over structured transition labels, then run it online with latest-write retrieval on held-out countdown, branch, and memory programs | validated for current toy programs |
| C2d | The current neural event branch is still narrower than a token-level executor | Check whether the learned branch depends only on opcode-conditioned structured labels rather than a richer event-level or token-level context model | validated as a limitation |
| C2e | Direct event-value decoding is a harder problem than opcode-conditioned rule decoding | Train a history-aware factorized event-value decoder, then compare teacher-forced label accuracy with free-running rollout on train and held-out program families | partially validated as a failure mode |
| C2f | A staged pointer-like decoder can close much of the direct-decoder rollout gap on the current toy families, while exposing how much mask strength matters | Train a recent-history candidate-source decoder, then compare structural, `opcode_shape`, and `opcode_legal` rollout on countdown, branch, indirect-memory, ping-pong, and `alternating_memory_loop` programs | validated for the current toy families, with explicit mask-strength caveats |
| C2g | The staged pointer label space still does not rescue the standard softmax baseline under scientifically comparable decode regimes | Train the same pointer-space target with the small standard softmax baseline, then evaluate structural and `opcode_shape` rollout separately from the `opcode_legal` diagnostic | validated as a negative control on the current toy families |
| C2h | The remaining staged-pointer gap can be attributed to specific failure families rather than vague “more learning needed” explanations | Build a per-family failure taxonomy across `structural`, `opcode_shape`, and `opcode_legal`, then decide whether any extra intermediate regime is justified | validated as a negative closure on the expanded suite |
| C3 | Finite-precision 2D addressing remains stable at practically relevant scales | Quantized parabolic latest-write stress tests across float64/32/bfloat16/float16 | partially validated with clear collapse limits |
| C3a | The current single-head float32 latest-write encoding collapses early | Local sweep over the single-head scheme shows the first float32 latest-write failure by address limit `512`, with stability only through `256` | validated |
| C3b | Simple decomposition schemes materially extend float32 latest-write stability | Local sweep over `radix2` and `block_recentered` schemes extends float32 latest-write stability from `256` to `4096` | validated for current synthetic sweeps |
| C3c | Real trace streams exhibit the same finite-precision pathology once address magnitude is high enough | Offset real-trace checks over loop and mixed stack-memory programs under single-head and decomposed schemes | validated for current offset suites |
| C3d | Horizon inflation on real traces reveals additional float32 single-head failures even when the native stream still passes | Re-evaluate the same real streams under larger `max_steps` budgets and sweep decomposition bases | validated for the current float32 offset sweep |
| C3e | The precision story can be widened beyond the current offset-derived suite and summarized by a stable failure taxonomy | Expand the real-trace suite with organically longer and branch-heavy streams, then classify failures rather than only counting pass/fail | partially validated on the broadened current suite |
| C3f | On the active Origin-core bundle, long-horizon inflation sharpens a narrow precision boundary without changing scope governance | `R36_origin_long_horizon_precision_scaling_gate` row-level screening over high-address memory, deep stack, and call/control traces | validated as a narrow boundary: float32 `single_head` shows inflated-horizon `tie_collapse` on selected rows while `radix2` and `block_recentered` recover those rows |
| H29 | The post-`R34/R35` Origin-core chain can be frozen positively without reopening blocked lanes | `H29_refreeze_after_r34_r35_origin_core_gate` packet over `H28/R34/R35` summaries and milestone docs | validated on the current chain state; keeps `R29/F3` blocked and routes only to narrow `R36` |
| D1a | One tiny lowered source subset can target the active Origin-core substrate exactly | `R37_origin_compiler_boundary_gate` over a fixed bytecode subset with source reference, lowered parity, and free-running exact execution | validated narrowly on the admitted bytecode subset only; not arbitrary `C`, broad compiler coverage, or a systems claim |
| H30 | The post-`R36/R37` Origin-core state can be refrozen without scope lift | `H30_post_r36_r37_scope_decision_packet` over `H29/R36/R37` summaries, milestone docs, and canonical handoff files | validated; keeps `R29/F3` blocked and requires a later explicit packet before any compiled-boundary extension |
| H31 | The post-`H30` fork can be resolved by one explicit same-substrate extension decision | `H31_post_h30_later_explicit_boundary_decision_packet` over `H30/R37` summaries, saved plan, and milestone docs | validated as a docs-only packet; authorizes exactly one admitted richer control/call family plus one non-admission boundary probe while keeping `R29/F3` blocked |
| D1b | One richer compiled control/call family can target the same active Origin-core substrate without widening the opcode surface | `R38_origin_compiler_control_surface_extension_gate` over `subroutine_braid` admitted and boundary rows with source reference, lowered parity, and free-running exact execution | validated narrowly on the admitted `subroutine_braid` family and one longer same-family boundary probe only; not arbitrary `C`, broad compiler coverage, or a systems claim |
| H32 | The post-`R38` Origin-core state can be refrozen without broader compiler scope lift | `H32_post_r38_compiled_boundary_refreeze` over `H30/H31/R38` summaries, milestone docs, and canonical handoff files | validated; keeps one richer compiled control family narrow, preserves `R29/F3` blocked, and requires a new plan before any further compiled-boundary widening |
| H34 | The post-`R39` compiled-boundary line can be frozen complete-for-now without another same-substrate reopen | `H34_post_r39_later_explicit_scope_decision_packet` over `H33/R39/H32` summaries, milestone docs, and canonical handoff files | validated; keeps `H32` as preserved routing, selects `freeze_compiled_boundary_as_complete_for_now`, and authorizes no new runtime lane |
| H35 | One bounded richer same-substrate runtime question can be authorized explicitly without automatic reopen pressure | `H35_post_p23_bounded_scalar_family_runtime_decision_packet` over `F13/F14`, `H34`, and the bounded-scalar runtime design | validated as a docs-only packet; authorizes exactly one bounded-scalar runtime gate and leaves `R41` deferred |
| D1c | One bounded scalar locals-and-flags family beyond the prior executor floor can target the current substrate exactly | `R40_origin_bounded_scalar_locals_and_flags_gate` over one admitted row, one same-family boundary row, and declared negative controls | validated narrowly on the landed bounded-scalar pair only; not broader value semantics, restricted Wasm, or arbitrary `C` |
| H36 | The post-`R40` bounded-scalar state can be refrozen narrowly with no active downstream runtime lane | `H36_post_r40_bounded_scalar_family_refreeze` over `H35/R40` summaries, milestone docs, and current handoff files | validated; preserves the bounded-scalar result narrowly, keeps `R41` deferred, and returns the stack to `no_active_downstream_runtime_lane` |
| H37 | The post-`H36` runtime-relevance decision can keep the current freeze by default when no uniquely isolated admissible contradiction survives | `H37_post_h36_runtime_relevance_decision_packet` over `H36`, `P25`, `F15`, `F14`, and the saved `R41` design | validated as a docs-only packet; preserves `H36`, selects `keep_h36_freeze`, and keeps `R41` deferred behind a later explicit packet |
| H38 | The post-`F16` runtime-relevance reopen decision can keep the current freeze when the saved `R41` catalog still yields zero execution-ready candidates | `H38_post_f16_runtime_relevance_reopen_decision_packet` over `F16`, `H37`, `H36`, and the saved `R41` design | validated as a docs-only packet; preserves `H36`, records `F16` as `no_candidate_ready`, selects `keep_h36_freeze`, and keeps `R41` deferred behind a later explicit packet |
| H40 | The post-`H38` semantic-boundary route can be activated only by a later explicit packet that authorizes exactly `R42` | `H40_post_h38_semantic_boundary_activation_packet` over `H38`, `F18`, `F19`, and the saved semantic-boundary gate ladder | validated as a docs-only packet; preserves `H36`, keeps `H38` visible as the prior keep-freeze decision, authorizes exactly `R42`, and keeps `R41/R43/R44` deferred |
| H41 | The post-`R42` aggressive long-arc route can be reactivated only by a later explicit packet that authorizes exact `R43` plus coequal model `R45` under an explicit evidence boundary | `H41_post_r42_aggressive_long_arc_decision_packet` over `H40`, `R42`, `F20`, and the saved post-`R42` control-override design | validated as a docs-only packet; preserves `H36`, keeps `H40` visible as the preserved prior activation packet, authorizes exact `R43`, admits coequal `R45`, keeps `R41` deferred, and keeps `R44` behind later `H42` |
| H41 | The post-`R42` route can be advanced only by a later explicit packet that authorizes exact `R43` while admitting coequal model `R45` under an explicit evidence boundary | `H41_post_r42_aggressive_long_arc_decision_packet` over `R42`, `F20`, `H40`, `F18`, and `F19` | validated as a docs-only packet; preserves `H36`, keeps `H40` visible as the preserved prior activation packet, authorizes exact `R43`, admits coequal model `R45`, keeps `R41` deferred, and keeps `R44` behind later `H42` |
| H42 | The post-`R43/R45` bounded-memory line can advance to restricted useful-case execution only by a later explicit packet that preserves the exact-model boundary and fixed `F19` ladder | `H42_post_r43_route_selection_packet` over `H41`, `R43`, `R45`, `F19`, `F20`, and `P27` | validated as a docs-only packet; preserves `H41/H36`, authorizes exactly `R44`, keeps `R41` deferred, and keeps merge posture operational rather than scientific |
| H43 | The post-`R44` semantic-boundary line can be refrozen as narrow useful-case support without authorizing a broader runtime successor | `H43_post_r44_useful_case_refreeze` over `H42`, `R43`, `R44`, `R45`, `F18`, `F19`, `F20`, and `P27` | validated as a docs-only packet; preserves `H42/H36`, records claim `D` as `supported_here_narrowly`, keeps `R41` deferred, and restores `no_active_downstream_runtime_lane` |
| F21 | The post-`H43` useful-case line can continue only through an exact-first planning bundle that keeps `H43` as the paper-grade endpoint and fixes `R46` as the first admissible next runtime candidate | `F21_post_h43_exact_useful_case_expansion_bundle` over `H43`, `R44`, `R45`, `P27`, and the post-`H43` reentry master plan | validated as a planning-only bundle; preserves `H43` as the current paper-grade endpoint, keeps `R47/R48` conditional, and does not authorize execution by itself |
| H44 | The post-`H43` stack can reauthorize exactly `R46` without widening the paper-grade endpoint or merge posture | `H44_post_h43_route_reauthorization_packet` over `H43`, `F21`, `R43`, `R44`, `R45`, and `P27` | validated as a docs-only packet; preserves `H43` as the paper-grade endpoint, authorizes exactly `R46`, keeps `R47/R48` deferred, and leaves `merge_executed = false` |
| H45 | The post-`R46` useful-case line can authorize exactly one restricted frontend bridge only after explicit interpretation of the landed narrow generalization result | `H45_post_r46_surface_decision_packet` over `H44`, `R46`, `H43`, `R43`, `R44`, `R45`, `F21`, and `P27` | validated as a docs-only packet; preserves `H43` as the paper-grade endpoint, preserves `H44` as the prior route packet, authorizes exactly `R47`, keeps `R48` conditional behind later `H46`, and leaves `merge_executed = false` |
| H46 | The post-`R47` useful-case line can authorize exactly one comparator-only model gate only after explicit interpretation of the landed narrow frontend bridge result | `H46_post_r47_frontend_bridge_decision_packet` over `H45`, `R47`, `H43`, `R46`, `F21`, `F22`, and `P27` | validated as a docs-only packet; preserves `H43` as the paper-grade endpoint, preserves `H45` as the prior decision packet, authorizes exactly `R48`, keeps model evidence comparator-only and non-substitutive, and leaves `merge_executed = false` |
| H47 | The post-`R48` useful-case line can refreeze comparator-only support without authorizing a new runtime lane | `H47_post_r48_useful_case_bridge_refreeze` over `H46`, `R48`, `H43`, `R46`, `R47`, `F21`, `F22`, and `P27` | validated as a docs-only packet; preserves `H43` as the paper-grade endpoint, preserves `H46` as the prior decision packet, freezes `R48` as narrow comparator-only support, restores `no_active_downstream_runtime_lane`, and leaves `merge_executed = false` |
| F23 | The post-`H47` useful-case line can continue only through one planning-only numeric-scaling bundle that fixes `R49` as the only admissible next runtime candidate while preserving the exact-versus-comparator boundary | `F23_post_h47_numeric_scaling_bundle` over `H47`, `R46`, `R47`, `R48`, `P35`, and the post-`H47` extension master plan | validated as a planning-only bundle; preserves `H47` as the active docs-only packet, preserves `H43` as the paper-grade endpoint, fixes `R49` as the only next runtime candidate, keeps exact `R46/R47` evidence decisive relative to comparator-only `R48`, keeps `F24` dormant, and leaves `F25/P36` as placeholders only |
| D2a | The fixed useful-case ladder generalizes narrowly inside the same declared restricted surface | `R46_origin_useful_case_surface_generalization_gate` over eight held-out in-surface variants spanning the fixed `R44` three-kernel ladder | validated narrowly on `8/8` held-out variants across `3/3` kernels; still bounded useful-case only and not arbitrary `C` or broader Wasm |
| D2b | One structured static-memory frontend can lower exactly onto the preserved useful-case kernels without widening the runtime claim ceiling | `R47_origin_restricted_frontend_translation_gate` over eight held-out restricted-frontend variants spanning the fixed `R44` three-kernel ladder | validated narrowly on `8/8` held-out variants across `3/3` kernels with `translation_identity_exact_count = 8`; still bounded useful-case only and not arbitrary `C`, broader Wasm, or a new runtime stack |
| D2c | A comparator-only dual-mode useful-case model lane can stay exact on the preserved useful-case contract without replacing exact evidence | `R48_origin_dual_mode_useful_case_model_gate` over `compiled_weight_executor` and `trainable_2d_executor` on the fixed `R47` useful-case contract, with held-out trainable evaluation on `histogram16_u8` | validated narrowly on `2/2` admitted modes across `8/8` preserved useful-case variants, with the trainable mode also exact on the explicit held-out `histogram16_u8` family (`3/3` variants); remains comparator-only, bounded to useful cases, and non-substitutive relative to exact `R46/R47` evidence |
| D2d | The preserved useful-case ladder survives one more narrow numeric-scaling widening under admitted float32 recovery regimes without scope lift | `R49_origin_useful_case_numeric_scaling_gate` over the saved `F23` three-bucket ladder on `sum_i32_buffer`, `count_nonzero_i32_buffer`, and `histogram16_u8` | validated narrowly on `9/9` widened variants across `3/3` kernels; `float32_single_head` fails on `7/9` rows by `tie_collapse` while both admitted recovery regimes (`radix2`, `block_recentered`) stay exact on all buckets; still bounded useful-case only and not arbitrary `C` or broader Wasm |
| D2e | One restricted tiny-`C` lowering surface can target the preserved useful-case kernels exactly without widening the runtime claim ceiling | `R50_origin_restricted_tinyc_lowering_gate` over the admitted single-function static tiny-`C` surface spanning the fixed `R47` useful-case contract | validated narrowly on `8/8` admitted tiny-`C` variants across `3/3` kernels with `translation_identity_exact_count = 8`; remains bounded to the declared single-function static `i32` surface with external layout metadata, and does not authorize arbitrary `C`, broader Wasm, or scope lift |
| H49 | The post-`R50` useful-case line can freeze exact restricted tiny-`C` support narrowly without authorizing a broader downstream runtime lane | `H49_post_r50_tinyc_lowering_decision_packet` over `H48`, `F25`, `R50`, `H43`, `P35`, and `P27` | validated as a docs-only packet; preserves `H43` as the paper-grade endpoint, preserves `H48` as the prior decision packet, records `R50` as the completed current restricted tiny-`C` lowering gate, selects `freeze_r50_as_narrow_exact_tinyc_support_only`, leaves `treat_r50_as_scope_widening_authorization` non-selected, restores `no_active_downstream_runtime_lane`, and sets `next_required_lane = no_active_downstream_runtime_lane` |
| F26 | The post-`H49` Origin-core line can continue only through one planning-only claim-delta bundle that preserves the narrow exact stack, fixes exactly one substrate-sufficiency runtime question, and defers broader growth behind a later explicit packet | `F26_post_h49_origin_claim_delta_and_next_question_bundle` over `H49`, `R50`, `H43`, `H36`, `P35`, and the post-`H49` next-wave design | validated as a planning-only bundle; preserves `H49` as the active docs-only packet, preserves `H43` as the paper-grade endpoint, preserves `H36` as the routing/refreeze packet, maps claim layers `A/B/C/D` onto the landed evidence stack, fixes `R51` as the only next runtime candidate, fixes `R52` as the only later comparator/value gate, fixes `H50` as the only follow-up packet, and keeps `F27` blocked behind later explicit positive `H50` |
| D2f | The landed exact substrate remains sufficient on one bounded richer memory/control surface beyond the prior tiny useful-case rows | `R51_origin_memory_control_surface_sufficiency_gate` over five predeclared post-`R50` families spanning delayed overwrite recovery, deeper stack-relative reads, loop-carried state, nested call/return, and one bounded lowered tiny-`C` row | validated narrowly on `5/5` exact families with `5/5` maximizer-row identity passes and `5/5` budget-clean rows; remains bounded to the declared exact memory/control surface and does not authorize arbitrary `C`, broader Wasm, or trainable/transformed growth |
| D2g | The current internal exact route should retain bounded system value over simpler baselines on the landed `R51` rows | `R52_origin_internal_vs_external_executor_value_gate` over accelerated internal exact, internal linear/reference, and plain external interpreter baselines on the exact `R51` rows | disconfirmed on the current bounded row set: all comparators stay exact on `5/5` rows, but accelerated internal exact beats linear/reference on only `3/5` rows and beats the external interpreter on `0/5` rows, while the external interpreter remains materially faster and operationally simpler |
| H50 | The post-`R51/R52` Origin-core line can stop as exact without system value when bounded substrate sufficiency survives but bounded comparator value fails | `H50_post_r51_r52_scope_decision_packet` over `H49`, `F26`, `R51`, `R52`, `H43`, and `P36` | validated as a docs-only packet; preserves `H49` as the prior decision packet, preserves `H43` as the paper-grade endpoint, selects `stop_as_exact_without_system_value`, leaves `freeze_as_narrow_specialized_executor_only` and `allow_planning_only_f27_entry_bundle` non-selected, restores `no_active_downstream_runtime_lane`, and keeps `F27` blocked |
| F28 | The post-`H50` Origin-core line can reopen only through one planning-only mechanism reentry bundle that preserves the negative broader-route closeout, fixes `H51` as the only follow-up packet, and fixes `R55 -> R56 -> R57 -> H52` as the only admissible sequence | `F28_post_h50_origin_mechanism_reentry_bundle` over `H50`, `H43`, `H36`, `R51`, `R52`, and the post-`H50` mechanism reentry master plan | validated as a planning-only bundle; preserves `H50` as the preserved prior closeout, preserves `H43` as the paper-grade endpoint, fixes `H51` as the only follow-up packet, fixes `R55` as the only next runtime candidate, keeps `R56/R57/H52` as the only later sequence, and keeps `F27/R53/R54` blocked |
| H51 | The post-`H50` Origin-core line can preserve the broader-route negative closeout while explicitly authorizing a narrower mechanism-first reentry through `R55` only | `H51_post_h50_origin_mechanism_reentry_packet` over `H50`, `F28`, `H43`, and the saved `R55/R56/R57/H52` lane | validated as a docs-only packet; preserves `H50` as the prior closeout, preserves `H43` as the paper-grade endpoint, selects `authorize_origin_mechanism_reentry_through_r55_first`, leaves terminal-stop and `F27` reactivation non-selected, fixes `R55` as the only next runtime candidate, and keeps `F27/R53/R54` blocked |
| D3a | The claimed `2D` hard-max retrieval path remains exactly equivalent to transparent latest-relevant-state lookup on the bounded mechanism-reentry suite | `R55_origin_2d_hardmax_retrieval_equivalence_gate` over overwrite-after-gap, deep stack-slot, duplicate-max identity, declared tie, and coordinate-offset rows | validated narrowly on `5/5` fixed tasks with `45/45` exact value observations, `45/45` exact maximizer-row identity observations, `2` exact tie observations, and `2` duplicate-max identity observations; establishes exact retrieval equivalence only, does not establish trace-VM semantics or system value, and makes `R56` the only next runtime candidate |
| D3b | Append-only trace plus exact retrieval can support one bounded free-running trace VM semantics contract exactly on the current mechanism-reentry suite | `R56_origin_append_only_trace_vm_semantics_gate` over static latest-write, loop-control, indirect-memory, call/return, and mixed-surface rows | validated narrowly on `5/5` fixed rows with exact step traces and exact final states on all rows, `288` exported transition rows, and exact free-running stack/memory/call retrieval (`206/52/2` reads respectively); establishes exact trace-VM semantics only, does not establish fast-path system value, and makes `R57` the only next runtime candidate |
| D3c | The accelerated trace-VM fast path should retain bounded value over transparent reference execution on the exact mechanism-reentry suite | `R57_origin_accelerated_trace_vm_comparator_gate` over accelerated internal trace-VM, linear internal trace-VM, and transparent external interpreter execution on the exact `R56` rows | disconfirmed on the current bounded row set: all comparator routes stay exact on `5/5` rows, but accelerated beats linear on `0/5` rows and beats the transparent external interpreter on `0/5` rows while internal retrieval share remains explicit (`260/288` read-to-transition ratio) |
| H52 | The post-`R55/R56/R57` mechanism lane can freeze as mechanism support without fast-path value while preserving the broader-route closeout and the paper-grade endpoint | `H52_post_r55_r56_r57_origin_mechanism_decision_packet` over `H50`, `H51`, `R55`, `R56`, `R57`, and `H43` | validated as a docs-only packet; preserves `H50` as the broader-route bounded-value falsifier, preserves `H43` as the paper-grade endpoint, records exact mechanism support through `R55/R56`, records negative fast-path value through `R57`, selects `freeze_origin_mechanism_supported_without_fastpath_value`, restores `no_active_downstream_runtime_lane`, and keeps `F27/R53/R54` blocked |
| F29 | The post-`H52` line can reopen only through one planning-only restricted compiled-boundary bundle that preserves the prior mechanism closeout, preserves the paper-grade endpoint, fixes `H53` as the only follow-up packet, and fixes `R58 -> R59 -> H54` as the only admissible sequence | `F29_post_h52_restricted_compiled_boundary_bundle` over `H52`, `H43`, `H36`, and the post-`H52` restricted compiled-boundary reentry master plan | validated as a planning-only bundle; preserves `H52` as the preserved prior closeout, preserves `H43` as the paper-grade endpoint, fixes `H53` as the only follow-up packet, fixes `R58` as the only next runtime candidate, keeps `R59/H54` as the only later sequence, and keeps `F27/R53/R54` blocked |
| H53 | The post-`H52` line can preserve the prior mechanism closeout while explicitly authorizing a narrower compiled-boundary reentry through `R58` only | `H53_post_h52_compiled_boundary_reentry_packet` over `H52`, `F29`, `H43`, and the saved compiled-boundary lane | validated as a docs-only packet; preserves the prior mechanism closeout while explicitly authorizing a narrower compiled-boundary reentry through `R58` only, preserves `H43` as the paper-grade endpoint, selects `authorize_compiled_boundary_reentry_through_r58_first`, fixes `R58` as the only next runtime candidate, and keeps `F27/R53/R54` blocked |
| R58 | One restricted typed stack-bytecode lowering surface can target the current substrate exactly on a fixed compiled-boundary suite without widening the claim ceiling | `R58_origin_restricted_stack_bytecode_lowering_contract_gate` over straight-line arithmetic, counted loop countdown, latest-write overwrite-after-gap, shallow call/return, and branch-fallthrough revisit rows | validated narrowly on `5/5` fixed rows across `5/5` categories with exact source/spec/lowered parity and exact lowering on the admitted typed stack-bytecode subset only; does not authorize arbitrary `C`, broad Wasm, or broader compiled scope lift |
| R59 | The lowered compiled trace-VM execution route can remain exact on the exact `R58` suite while still supporting only narrow compiled-boundary evidence | `R59_origin_compiled_trace_vm_execution_gate` over linear and accelerated internal lowered execution on the exact `R58` rows | validated narrowly on `5/5` exact lowered rows across `5/5` categories with exact source/lowered parity plus exact linear and accelerated internal lowered execution; supports only a narrow compiled-boundary reading and feeds `freeze_restricted_compiled_boundary_supported_narrowly_without_fastpath_value` rather than a broader systems claim |
| H54 | The post-`R58/R59` compiled-boundary lane can freeze as narrow restricted compiled-boundary support without fast-path value while preserving the prior mechanism closeout and the paper-grade endpoint | `H54_post_r58_r59_compiled_boundary_decision_packet` over `H52`, `H53`, `R58`, `R59`, and `H43` | validated as a docs-only packet; preserves `H52` as the prior mechanism closeout, preserves `H53` as the prior compiled-boundary reentry packet, preserves `H43` as the paper-grade endpoint, selects `freeze_restricted_compiled_boundary_supported_narrowly_without_fastpath_value`, restores `no_active_downstream_runtime_lane`, and keeps `F27/R53/R54` blocked |
| F30 | The post-`H54` line can continue only through one planning-only minimal useful-kernel bridge bundle that preserves the prior compiled-boundary closeout, preserves the paper-grade endpoint, fixes `H55` as the only follow-up packet, and fixes `R60 -> R61 -> H56` as the only admissible sequence | `F30_post_h54_useful_kernel_bridge_bundle` over `H54`, `H52`, `H43`, `H36`, and the post-`H54` useful-kernel stop/go design | validated as a planning-only bundle; preserves `H54` as the active compiled-boundary closeout at planning time, preserves `H52` as the prior mechanism closeout, preserves `H43` as the paper-grade endpoint, fixes `H55` as the only follow-up packet, fixes `R60` as the only next runtime candidate, fixes `R61/H56` as the only later sequence, keeps `P39` as the saved low-priority hygiene wave, and keeps `F27/R53/R54` blocked |
| H55 | The post-`H54` line can preserve the prior compiled-boundary closeout while explicitly authorizing one minimal useful-kernel carryover reentry through `R60` only | `H55_post_h54_useful_kernel_reentry_packet` over `H54`, `F30`, `H52`, and `H43` | validated as a docs-only packet; preserves `H54` as the prior compiled-boundary closeout, preserves `H52` as the prior mechanism closeout, preserves `H43` as the paper-grade endpoint, selects `authorize_useful_kernel_carryover_through_r60_first`, fixes `R60` as the only next runtime candidate, and keeps `F27/R53/R54` blocked |
| R60 | One restricted compiled tiny-`C` useful-kernel carryover surface can target the preserved useful-kernel pair exactly on the current compiled-boundary route without widening the claim ceiling | `R60_origin_compiled_useful_kernel_carryover_gate` over the admitted preserved `sum_i32_buffer` and `count_nonzero_i32_buffer` variants on the post-`H54` compiled route | validated narrowly on `5/5` admitted compiled useful-kernel variants across `2/2` preserved kernels with `translation_identity_exact_count = 5`, exact source/spec/lowered parity, exact linear and accelerated internal execution, and `compiler_work_leakage_break_count = 0`; remains bounded useful-case only and does not authorize arbitrary `C`, broader Wasm, or broader compiled growth |
| R61 | The exact compiled useful-kernel route should retain bounded value over simpler transparent baselines on the exact `R60` rows | `R61_origin_compiled_useful_kernel_value_gate` over accelerated internal compiled useful-kernel execution versus linear internal, lowered/source accounting, and plain external baselines on the exact `R60` rows | disconfirmed on the current bounded row set: all declared comparators stay exact on `5/5` rows, but accelerated internal execution is faster on `0/5` rows relative to linear, source, lowered, or external baselines once compiler/lowering overhead is counted |
| H56 | The post-`R60/R61` useful-kernel line can freeze minimal compiled useful-kernel bridge support without bounded value while preserving the prior compiled-boundary closeout and the paper-grade endpoint | `H56_post_r60_r61_useful_kernel_decision_packet` over `H54`, `H55`, `F30`, `R60`, `R61`, and `H43` | validated as a docs-only packet; preserves `H54` as the prior compiled-boundary closeout, preserves `H55` as the prior useful-kernel reentry packet, preserves `H43` as the paper-grade endpoint, selects `freeze_minimal_useful_kernel_bridge_supported_without_bounded_value`, restores `no_active_downstream_runtime_lane`, and keeps `F27/R53/R54` blocked |
| F31 | The post-`H56` line can continue only through one planning-only final discriminator bundle that preserves the closed compiled useful-kernel bridge, fixes `H57` as the only follow-up packet, and fixes `R62 -> H58` as the only admissible later sequence | `F31_post_h56_final_discriminating_value_boundary_bundle` over `H56`, `H43`, `H36`, and the post-`H56` last-discriminator design | validated as a planning-only bundle; preserves `H56` as the prior closeout, fixes `H57` as the only docs-only follow-up, fixes `R62` as the only next runtime candidate, fixes `H58` as the only later packet, keeps `P40` as the only low-priority sidecar, and keeps `F27/R53/R54` blocked |
| H57 | The post-`H56` line can preserve the prior closeout while explicitly authorizing one last native useful-kernel value discriminator through `R62` only | `H57_post_h56_last_discriminator_authorization_packet` over `H56`, `F31`, `H43`, and `H36` | validated as a docs-only packet; preserves `H56` as the prior closeout, preserves `H43` as the paper-grade endpoint, selects `authorize_one_last_native_useful_kernel_value_discriminator_gate`, fixes `R62` as the only next runtime candidate, and keeps later scope lift conditional on `H58` |
| R62 | Native useful-kernel programs should retain bounded value on the current append-only substrate when compiler/lowering overhead is removed from the measured executor comparator | `R62_origin_native_useful_kernel_value_discriminator_gate` over native `sum_i32_buffer` and `count_nonzero_i32_buffer` trace programs versus native linear, native accelerated, transparent trace interpretation, and scalar reference baselines | disconfirmed on the declared native row set: all declared comparators stay exact on `4/4` rows across `2/2` kernels, but accelerated execution is faster than linear on `0/2` longest kernel rows, reaches external-scalar same-order behavior on `0/2` kernels, and therefore does not retain bounded executor value even without compiler/lowering timing in the measured comparator |
| H58 | The post-`R62` line can close as mechanism support without bounded executor value while preserving the prior closeouts and the paper-grade endpoint | `H58_post_r62_origin_value_boundary_closeout_packet` over `H56`, `H57`, `F31`, `R62`, and `H43` | validated as a docs-only packet; preserves `H56` as the prior closeout, preserves `H57` as the prior authorization packet, preserves `H43` as the paper-grade endpoint, selects `stop_as_mechanism_supported_but_no_bounded_executor_value`, restores `no_active_downstream_runtime_lane`, and keeps `F27/R53/R54` blocked |
| F32 | The post-`H58` line can be certified as a real closed stop boundary that bars same-lane reopen without a new cost structure | `F32_post_h58_closeout_certification_bundle` over `H58`, `H43`, and the post-`H58` closeout analysis | validated as a planning-only certification bundle; preserves `H58` as the prior closeout, preserves `H43` as the paper-grade endpoint, routes only to `H59/P41/P42/F33`, and keeps `F27/R53/R54` blocked |
| H59 | The post-`H58` line can freeze as an explicit reproduction-gap packet that preserves the narrow positive result while requiring a materially different cost structure for any later reopen | `H59_post_h58_reproduction_gap_decision_packet` over `H58`, `F32`, `R62`, and `H43` | validated as a docs-only packet; preserves `H58` as the prior closeout, preserves `F32` as the prior certification, preserves `H43` as the paper-grade endpoint, selects `freeze_reproduction_gap_and_require_different_cost_structure_for_reopen`, and sets `current_downstream_scientific_lane = planning_only_or_project_stop` |
| F33 | The post-`H59` line can continue only through planning-only different-cost-structure candidate screening or explicit project stop | `F33_post_h59_different_cost_structure_reopen_bundle` over `H59`, `P42`, and the post-`H59` planning design | validated as a planning-only bundle; preserves `H59` as the active packet, keeps `P42` as the current low-priority dossier sidecar, requires `materially_different_cost_structure`, keeps `F27/R53/R54` blocked, and keeps project stop admissible |
| P43 | The post-`H59` line can record repo-local worktree graph and explicit no-merge posture without reopening science | `P43_post_h59_repo_graph_hygiene_and_merge_map` over `git worktree list --porcelain`, current branch identity, and the repo-local alias rule | validated as an operational sidecar; keeps dirty root `main` quarantined, records `clean_descendant_only_never_dirty_root_main`, and treats merge posture as hygiene rather than evidence |
| F34 | The post-`H59` line can screen exactly one conditionally credible later reopen family without opening a runtime lane | `F34_post_h59_compiled_online_retrieval_reopen_screen` over `H59`, `F33`, and `P42` | validated as a planning-only bundle; preserves `H59` as the prior reproduction-gap packet, names only `compiled_online_exact_retrieval_primitive_or_attention_coprocessor_route`, keeps same-lane executor-value microvariants inadmissible, and keeps `planning_only_or_project_stop` as the default fallback |
| F35 | The post-`H59` line can store high-cost model and programs-into-weights ideas as far-future only without promoting them into current execution candidates | `F35_post_h59_far_future_model_and_weights_horizon_log` over `H59` and `F34` | validated as a planning-only storage bundle; keeps both routes far-future only, keeps current execution candidate count at zero, and preserves `no_runtime_lane_open_until_later_explicit_authorization` |
| H60 | The post-`F34` line can freeze as planning-only / archive / stop while preserving the prior reproduction-gap packet and one conditional future family | `H60_post_f34_next_lane_decision_packet` over `H59`, `F34`, `P43`, and `F35` | validated as a docs-only packet; preserves `H59` as the prior reproduction-gap packet, preserves `H58/F32/H43`, selects `remain_planning_only_and_prepare_stop_or_archive`, keeps `planning_only_or_project_stop`, and does not authorize runtime execution |
| P44 | The post-`H59` line can lock outward wording to the honest archive state without widening claims or reactivating the closed lane | `P44_post_h59_publication_surface_and_claim_lock` over `H60`, `F34`, and publication-facing surfaces | validated as an operational/docs wave; keeps the narrow positive explicit, keeps the broad headline negative explicit, keeps same-lane executor-value microvariants inadmissible, and keeps the compiled-online route conditional only |
| P45 | The post-`H60` line can record clean-descendant integration readiness without claiming a merge or routing through dirty root `main` | `P45_post_h60_clean_descendant_integration_readiness` over `H60`, `P43`, `git worktree list --porcelain`, and the current branch identity | validated as a preserved prior operational sidecar; preserves `clean_descendant_only_never_dirty_root_main`, records the clean successor line, and keeps merge posture operational rather than scientific |
| F36 | The post-`H60` line can keep exactly one future reopen family alive only as a qualification-only dossier with strict pre-runtime gates | `F36_post_h60_conditional_compiled_online_reopen_qualification_bundle` over `H60`, `F34`, and `F35` | validated as a preserved prior planning-only bundle; preserves the single compiled-online family, fixes transparent baseline and useful-case requirements, keeps runtime closed, and rejects renamed same-lane executor-value work |
| H61 | The post-`H60` line can make archive-first consolidation the default live posture while preserving all prior closeout packets underneath it | `H61_post_h60_archive_first_position_packet` over `H60`, `P45`, `F36`, and `F35` | validated as a preserved prior docs-only packet; preserves `H60/H59/H58/F32/H43`, selects `archive_first_consolidation_becomes_default_posture`, keeps `planning_only_or_project_stop`, and does not authorize runtime execution |
| P46 | The post-`H60` line can lock outward wording to archive-first plus executor-value partial falsification framing without widening claims | `P46_post_h60_archive_first_publication_sync` over `H61`, `F36`, and publication-facing surfaces | validated as a preserved prior operational/docs wave; keeps narrow positive mechanism wording explicit, keeps broader headline failure explicit, removes stale multi-patch reopen wording, and keeps the compiled-online route qualification-only |
| P47 | The post-`H61` line can quarantine the dirty root checkout and keep merge-main discussion planning-only | `P47_post_h61_root_quarantine_and_main_merge_planning` over `H61`, root parking branch identity, parked-root status rows, and merge-base against the clean descendant | validated as an operational sidecar; records the parked root checkout as quarantine-only, keeps dirty root `main` out of integration, and preserves merge-main as planning-only rather than executed |
| P48 | The post-`H61` line can make clean-descendant promotion prep explicit without claiming promotion execution | `P48_post_h61_clean_descendant_promotion_prep` over `P45`, `P47`, current branch identity, and descendant diff metadata | validated as an operational sidecar; preserves descendant-only promotion posture, records source/current branch roles plus oversized-artifact posture, and separates explicit prep from merge execution |
| P49 | The post-`H61` line can sync text-only Origin and GPTPro advisory material into the clean descendant without changing evidence status | `P49_post_h61_origin_advisory_sync` over `H61`, `P42`, and the synced `docs/Origin/*.md` advisory files | validated as an operational/docs wave; syncs six markdown advisory files, preserves advisory-only status, and removes dependence on the parked root checkout for later planning input |
| F37 | The post-`H61` line can keep exactly one future route alive only as a stricter compiled-online coprocessor reauthorization bundle with runtime still closed | `F37_post_h61_compiled_online_coprocessor_reauthorization_bundle` over `H61`, `F36`, `P49`, and the narrowed milestone contract | validated as a planning-only bundle; narrows the surviving family to `compiled_exact_hardmax_attention_coprocessor_on_lifted_useful_kernel_decode`, fixes three honest baselines plus five eligibility gates and seven stop rules, and does not authorize runtime |
| H62 | The post-`P47/P48/P49/F37` line can keep archive/hygiene stop as the default downstream state while limiting any continuation to one conditional non-runtime profile gate | `H62_post_p47_p48_p49_f37_hygiene_first_scope_decision_packet` over `H61`, `P47`, `P48`, `P49`, and `F37` summaries | validated as a docs-only packet; preserves `H61/H60/H59/H58/F32/H43`, selects `hygiene_first_scope_decision_keeps_archive_default_and_limits_any_reopen_to_r63_profile_gate`, keeps runtime closed, and limits any continuation to conditional non-runtime `R63` profiling |
| P50 | The post-`H62` line can synchronize top-level control surfaces to archive-first closeout without reopening runtime | `P50_post_h62_archive_first_control_sync` over `H62`, root/publication driver docs, and the active-wave control surfaces | validated as the current control sync wave; locks the control surfaces to archive-first closeout, keeps `archive_or_hygiene_stop` as default, and keeps `R63` dormant |
| P51 | The post-`H62` line can package the strongest honest paper-facing reading as archive-first partial falsification without claiming runtime superiority | `P51_post_h62_paper_facing_partial_falsification_package` over `H62`, `P50`, `F37`, and publication/review/archive ledgers | validated as the current paper-facing package wave; keeps narrow positive mechanism support explicit, keeps broad headline reproduction negative, and keeps the only future route dormant |
| P52 | The post-`H62` line can refresh clean-descendant hygiene and merge-prep without routing through dirty root `main` | `P52_post_h62_clean_descendant_hygiene_and_merge_prep` over `H62`, `P47`, `P48`, worktree state, and raw-row artifact posture | validated as the current repo-hygiene sidecar; preserves `clean_descendant_only_never_dirty_root_main`, keeps root quarantine explicit, and does not execute a merge |
| F38 | The post-`H62` line can preserve the only surviving future family only as a dormant non-runtime dossier until its missing cost-profile fields are resolved honestly | `F38_post_h62_r63_dormant_eligibility_profile_dossier` over `H62`, `F37`, the exact useful target, and the saved eligibility-gap/no-go wording | validated as the current dormant future dossier; names the exact lifted useful target, leaves cost share/query ratio/tie burden/material cost delta unresolved, and keeps runtime closed |
| H63 | The post-`P50/P51/P52/F38` line can make archive-first closeout the active repo route while preserving `H62` as the prior active packet | `H63_post_p50_p51_p52_f38_archive_first_closeout_packet` over `H62`, `P50`, `P51`, `P52`, and `F38` | validated as the current active docs-only packet; selects `archive_first_closeout_becomes_current_active_route_and_r63_stays_dormant`, keeps `archive_or_hygiene_stop` as the default downstream lane, and keeps `R63` non-runtime only |
| H48 | The post-`R49` useful-case line can authorize exactly one restricted tiny-`C` lowering planning bundle or freeze as a practical falsifier, with the landed packet selecting the positive planning-only route | `H48_post_r49_numeric_scaling_decision_packet` over `H47`, `F23`, `R49`, `H43`, `P35`, and `P27` | validated as a docs-only packet; preserves `H43` as the paper-grade endpoint, preserves `H47` as the prior decision packet, records `R49` as the completed current numeric-scaling gate, selects `authorize_f25_restricted_tinyc_lowering_bundle`, leaves `P36` explicit but non-selected, restores `no_active_downstream_runtime_lane`, and sets `next_required_lane = f25_post_h48_restricted_tinyc_lowering_bundle` |
| F25 | The post-`H48` useful-case line can continue only through one planning-only restricted tiny-`C` lowering bundle that narrows the earlier `R47` frontend bridge into one explicit `R50` runtime question | `F25_post_h48_restricted_tinyc_lowering_bundle` over `H48`, `R49`, `R47`, `F21`, `F19`, `H43`, and `P35` | validated as a planning-only bundle; preserves `H48` as the current active docs-only packet, preserves `H43` as the paper-grade endpoint, records `R49` as completed current evidence, narrows the admitted surface to bounded `i32` scalars, declared static buffers, structured control flow, fixed-range table updates, and one top-level kernel function, fixes `R50` as the only next runtime candidate, fixes `H49` as the only follow-up packet, and keeps `claim_ceiling = bounded_useful_cases_only` |
| D0 | The first compiled frontend boundary is now a preserved historical endpoint rather than the active mainline | `H27_refreeze_after_r32_r33_same_endpoint_decision`, `H28_post_h27_origin_core_reanchor_packet`, and `H29_refreeze_after_r34_r35_origin_core_gate` | preserved as historical narrow boundary; same-endpoint recovery remains closed while active routing stays on Origin-core substrate gates |
| D1 | A higher-level compiled pipeline can target this substrate | Restricted Wasm-like or tiny C-like compiler path | out of scope for the current Origin-core refreeze; any scope lift still needs a later explicit packet after `R36` |
