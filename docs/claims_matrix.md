# Claims Matrix

| Claim | Minimal technical interpretation | Experiment | Status |
| --- | --- | --- | --- |
| A1 | Computation can be encoded as an append-only trace | Reference interpreter plus replay engine agree on final state | validated for current toy programs |
| A2 | Useful state can be recovered from bounded trace-local semantics | Trace events expose explicit pops, pushes, branches, next-PC transitions, and latest memory writes | validated for current stack + bounded RAM programs |
| B1 | Exact 2D hard-max retrieval can be implemented exactly | Brute-force reference path over 2D keys/values/queries | validated on current tests |
| B2 | A specialized cache can answer the same retrieval sublinearly | `HullKVCache` query agrees with brute force and benchmarks better than linear scan | validated for current benchmark sizes |
| B3 | Tie handling is part of semantics, not a detail | Exact averaging over equal maximizers, including degeneracies | validated on current tie tests |
| B4 | The minimal Origin-core retrieval primitives remain exact on real extracted traces | `R34_origin_retrieval_primitive_contract_gate` over latest-write, stack top/depth, call-return target, and tie semantics | validated on the current narrow primitive suite |
| B5 | The first semantic-boundary retrieval contract preserves exact value and maximizer-row identity on fixed append-only memory tasks | `R42_origin_append_only_memory_retrieval_contract_gate` over latest-write-by-address, stack-slot depth, duplicate/tie, and range-sweep rows | validated on six fixed contract tasks only; does not yet validate bounded-memory VM execution, coequal model mainline execution, or useful-case kernels |
| C1 | These primitives can support a small exact executor | Tiny stack-machine interpreter, replay engine, and bounded RAM latest-write semantics | validated on the current narrow suite |
| C1a | Latest-write memory reads can be rewritten as exact 2D hard-max causal retrieval | Linear scan and `HullKVCache` agree on extracted memory-read events from real traces | validated for immediate and dynamic bounded RAM examples |
| C1b | Stack-slot reads can be rewritten as exact 2D hard-max causal retrieval | Linear scan and `HullKVCache` agree on extracted stack-slot read events from real traces | validated for current stack-machine examples |
| C1c | A narrow trainable scorer can recover latest-write stack retrieval with the same inductive bias | Fit a two-parameter scorer on short countdown stack traces, then evaluate on longer countdown traces and a dynamic-memory stack trace | validated for current toy traces |
| C1d | An online executor can recover value state from append-only latest-write history during rollout | Free-running exact linear and accelerated executors reproduce the reference trace on current stack, branch, and bounded-RAM programs | validated for current toy programs |
| C1e | Call/return targets can be expressed as append-only latest-write frame retrieval | `R34` call-frame primitive audit over the nested `call_chain` trace | validated for the current narrow call chain |
| C1f | A retrieval-backed executor can keep call/return state without a hidden Python-only call-stack side channel | `R35_origin_append_only_stack_vm_execution_gate` pointer-like exact rollout on the current stack-VM bundle | validated for the current narrow execution bundle |
| C2 | The executor survives longer free-running rollout on a mixed narrow stack-VM bundle | `R35` exact trace/final-state evaluation on straight-line, loop, indirect-memory, control-flow, mixed-surface, and call/return cases | validated for the current narrow bundle |
| C2a | A narrow learned retrieval rule can stay exact in free-running rollout on current stack-family programs | Fit on short countdowns, then run the scorer online in held-out countdown, branch, and current bounded-RAM traces | validated for current toy programs |
| C2b | A trace-induced structured executor can generate event decisions online without hand-coded opcode semantics | Fit per-opcode transition rules from reference traces, then run them online with latest-write retrieval on held-out countdown, branch, and indirect-memory programs | validated for current toy programs |
| C2c | A neural causal executor can generate event decisions online without exact fallback | Train an opcode-conditioned neural decoder over structured transition labels, then run it online with latest-write retrieval on held-out countdown, branch, and memory programs | validated for current toy programs |
| C2d | The current neural event branch is still narrower than a token-level executor | Check whether the learned branch depends only on opcode-conditioned structured labels rather than a richer event-level or token-level context model | validated as a limitation |
| C2e | Direct event-value decoding is a harder problem than opcode-conditioned rule decoding | Train a history-aware factorized event-value decoder, then compare teacher-forced label accuracy with free-running rollout on train and held-out program families | partially validated as a failure mode |
| C2f | A staged pointer-like decoder can close much of the direct-decoder rollout gap on the current toy families, while exposing how much mask strength matters | Train a recent-history candidate-source decoder, then compare structural, `opcode_shape`, and `opcode_legal` rollout on countdown, branch, indirect-memory, ping-pong, and `alternating_memory_loop` programs | validated for the current toy families, with explicit mask-strength caveats |
| C2g | The staged pointer label space still does not rescue the standard softmax baseline under scientifically comparable decode regimes | Train the same pointer-space target with the small standard softmax baseline, then evaluate structural and `opcode_shape` rollout separately from the `opcode_legal` diagnostic | validated as a negative control on the current toy families |
| C2h | The remaining staged-pointer gap can be attributed to specific failure families rather than vague “more learning needed” explanations | Build a per-family failure taxonomy across `structural`, `opcode_shape`, and `opcode_legal`, then decide whether any extra intermediate regime is justified | validated as a negative closure on the expanded suite |
| C3 | Finite-precision 2D addressing remains stable at practically relevant scales | Quantized parabolic latest-write stress tests across float64/32/bfloat16/float16 | partially validated with clear collapse limits |
| C3a | The current single-head float32 latest-write encoding collapses early | Local sweep over the single-head scheme shows the first float32 latest-write failure by address limit `512`, with stability only through `256` | validated |
| C3b | Simple decomposition schemes materially extend float32 latest-write stability | Local sweep over `radix2` and `block_recentered` schemes extends float32 latest-write stability from `256` to `4096` | validated for current synthetic sweeps |
| C3c | Real trace streams exhibit the same finite-precision pathology once address magnitude is high enough | Offset real-trace checks over loop and mixed stack-memory programs under single-head and decomposed schemes | validated for current offset suites |
| C3d | Horizon inflation on real traces reveals additional float32 single-head failures even when the native stream still passes | Re-evaluate the same real streams under larger `max_steps` budgets and sweep decomposition bases | validated for the current float32 offset sweep |
| C3e | The precision story can be widened beyond the current offset-derived suite and summarized by a stable failure taxonomy | Expand the real-trace suite with organically longer and branch-heavy streams, then classify failures rather than only counting pass/fail | partially validated on the broadened current suite |
| C3f | On the active Origin-core bundle, long-horizon inflation sharpens a narrow precision boundary without changing scope governance | `R36_origin_long_horizon_precision_scaling_gate` row-level screening over high-address memory, deep stack, and call/control traces | validated as a narrow boundary: float32 `single_head` shows inflated-horizon `tie_collapse` on selected rows while `radix2` and `block_recentered` recover those rows |
| H29 | The post-`R34/R35` Origin-core chain can be frozen positively without reopening blocked lanes | `H29_refreeze_after_r34_r35_origin_core_gate` packet over `H28/R34/R35` summaries and milestone docs | validated on the current chain state; keeps `R29/F3` blocked and routes only to narrow `R36` |
| D1a | One tiny lowered source subset can target the active Origin-core substrate exactly | `R37_origin_compiler_boundary_gate` over a fixed bytecode subset with source reference, lowered parity, and free-running exact execution | validated narrowly on the admitted bytecode subset only; not arbitrary `C`, broad compiler coverage, or a systems claim |
| H30 | The post-`R36/R37` Origin-core state can be refrozen without scope lift | `H30_post_r36_r37_scope_decision_packet` over `H29/R36/R37` summaries, milestone docs, and canonical handoff files | validated; keeps `R29/F3` blocked and requires a later explicit packet before any compiled-boundary extension |
| H31 | The post-`H30` fork can be resolved by one explicit same-substrate extension decision | `H31_post_h30_later_explicit_boundary_decision_packet` over `H30/R37` summaries, saved plan, and milestone docs | validated as a docs-only packet; authorizes exactly one admitted richer control/call family plus one non-admission boundary probe while keeping `R29/F3` blocked |
| D1b | One richer compiled control/call family can target the same active Origin-core substrate without widening the opcode surface | `R38_origin_compiler_control_surface_extension_gate` over `subroutine_braid` admitted and boundary rows with source reference, lowered parity, and free-running exact execution | validated narrowly on the admitted `subroutine_braid` family and one longer same-family boundary probe only; not arbitrary `C`, broad compiler coverage, or a systems claim |
| H32 | The post-`R38` Origin-core state can be refrozen without broader compiler scope lift | `H32_post_r38_compiled_boundary_refreeze` over `H30/H31/R38` summaries, milestone docs, and canonical handoff files | validated; keeps one richer compiled control family narrow, preserves `R29/F3` blocked, and requires a new plan before any further compiled-boundary widening |
| H34 | The post-`R39` compiled-boundary line can be frozen complete-for-now without another same-substrate reopen | `H34_post_r39_later_explicit_scope_decision_packet` over `H33/R39/H32` summaries, milestone docs, and canonical handoff files | validated; keeps `H32` as preserved routing, selects `freeze_compiled_boundary_as_complete_for_now`, and authorizes no new runtime lane |
| H35 | One bounded richer same-substrate runtime question can be authorized explicitly without automatic reopen pressure | `H35_post_p23_bounded_scalar_family_runtime_decision_packet` over `F13/F14`, `H34`, and the bounded-scalar runtime design | validated as a docs-only packet; authorizes exactly one bounded-scalar runtime gate and leaves `R41` deferred |
| D1c | One bounded scalar locals-and-flags family beyond the prior executor floor can target the current substrate exactly | `R40_origin_bounded_scalar_locals_and_flags_gate` over one admitted row, one same-family boundary row, and declared negative controls | validated narrowly on the landed bounded-scalar pair only; not broader value semantics, restricted Wasm, or arbitrary `C` |
| H36 | The post-`R40` bounded-scalar state can be refrozen narrowly with no active downstream runtime lane | `H36_post_r40_bounded_scalar_family_refreeze` over `H35/R40` summaries, milestone docs, and current handoff files | validated; preserves the bounded-scalar result narrowly, keeps `R41` deferred, and returns the stack to `no_active_downstream_runtime_lane` |
| H37 | The post-`H36` runtime-relevance decision can keep the current freeze by default when no uniquely isolated admissible contradiction survives | `H37_post_h36_runtime_relevance_decision_packet` over `H36`, `P25`, `F15`, `F14`, and the saved `R41` design | validated as a docs-only packet; preserves `H36`, selects `keep_h36_freeze`, and keeps `R41` deferred behind a later explicit packet |
| H38 | The post-`F16` runtime-relevance reopen decision can keep the current freeze when the saved `R41` catalog still yields zero execution-ready candidates | `H38_post_f16_runtime_relevance_reopen_decision_packet` over `F16`, `H37`, `H36`, and the saved `R41` design | validated as a docs-only packet; preserves `H36`, records `F16` as `no_candidate_ready`, selects `keep_h36_freeze`, and keeps `R41` deferred behind a later explicit packet |
| H40 | The post-`H38` semantic-boundary route can be activated only by a later explicit packet that authorizes exactly `R42` | `H40_post_h38_semantic_boundary_activation_packet` over `H38`, `F18`, `F19`, and the saved semantic-boundary gate ladder | validated as a docs-only packet; preserves `H36`, keeps `H38` visible as the prior keep-freeze decision, authorizes exactly `R42`, and keeps `R41/R43/R44` deferred |
| H41 | The post-`R42` aggressive long-arc route can be reactivated only by a later explicit packet that authorizes exact `R43` plus coequal model `R45` under an explicit evidence boundary | `H41_post_r42_aggressive_long_arc_decision_packet` over `H40`, `R42`, `F20`, and the saved post-`R42` control-override design | validated as a docs-only packet; preserves `H36`, keeps `H40` visible as the preserved prior activation packet, authorizes exact `R43`, admits coequal `R45`, keeps `R41` deferred, and keeps `R44` behind later `H42` |
| H41 | The post-`R42` route can be advanced only by a later explicit packet that authorizes exact `R43` while admitting coequal model `R45` under an explicit evidence boundary | `H41_post_r42_aggressive_long_arc_decision_packet` over `R42`, `F20`, `H40`, `F18`, and `F19` | validated as a docs-only packet; preserves `H36`, keeps `H40` visible as the preserved prior activation packet, authorizes exact `R43`, admits coequal model `R45`, keeps `R41` deferred, and keeps `R44` behind later `H42` |
| H42 | The post-`R43/R45` bounded-memory line can advance to restricted useful-case execution only by a later explicit packet that preserves the exact-model boundary and fixed `F19` ladder | `H42_post_r43_route_selection_packet` over `H41`, `R43`, `R45`, `F19`, `F20`, and `P27` | validated as a docs-only packet; preserves `H41/H36`, authorizes exactly `R44`, keeps `R41` deferred, and keeps merge posture operational rather than scientific |
| H43 | The post-`R44` semantic-boundary line can be refrozen as narrow useful-case support without authorizing a broader runtime successor | `H43_post_r44_useful_case_refreeze` over `H42`, `R43`, `R44`, `R45`, `F18`, `F19`, `F20`, and `P27` | validated as a docs-only packet; preserves `H42/H36`, records claim `D` as `supported_here_narrowly`, keeps `R41` deferred, and restores `no_active_downstream_runtime_lane` |
| F21 | The post-`H43` useful-case line can continue only through an exact-first planning bundle that keeps `H43` as the paper-grade endpoint and fixes `R46` as the first admissible next runtime candidate | `F21_post_h43_exact_useful_case_expansion_bundle` over `H43`, `R44`, `R45`, `P27`, and the post-`H43` reentry master plan | validated as a planning-only bundle; preserves `H43` as the current paper-grade endpoint, keeps `R47/R48` conditional, and does not authorize execution by itself |
| H44 | The post-`H43` stack can reauthorize exactly `R46` without widening the paper-grade endpoint or merge posture | `H44_post_h43_route_reauthorization_packet` over `H43`, `F21`, `R43`, `R44`, `R45`, and `P27` | validated as a docs-only packet; preserves `H43` as the paper-grade endpoint, authorizes exactly `R46`, keeps `R47/R48` deferred, and leaves `merge_executed = false` |
| H45 | The post-`R46` useful-case line can authorize exactly one restricted frontend bridge only after explicit interpretation of the landed narrow generalization result | `H45_post_r46_surface_decision_packet` over `H44`, `R46`, `H43`, `R43`, `R44`, `R45`, `F21`, and `P27` | validated as a docs-only packet; preserves `H43` as the paper-grade endpoint, preserves `H44` as the prior route packet, authorizes exactly `R47`, keeps `R48` conditional behind later `H46`, and leaves `merge_executed = false` |
| H46 | The post-`R47` useful-case line can authorize exactly one comparator-only model gate only after explicit interpretation of the landed narrow frontend bridge result | `H46_post_r47_frontend_bridge_decision_packet` over `H45`, `R47`, `H43`, `R46`, `F21`, `F22`, and `P27` | validated as a docs-only packet; preserves `H43` as the paper-grade endpoint, preserves `H45` as the prior decision packet, authorizes exactly `R48`, keeps model evidence comparator-only and non-substitutive, and leaves `merge_executed = false` |
| H47 | The post-`R48` useful-case line can refreeze comparator-only support without authorizing a new runtime lane | `H47_post_r48_useful_case_bridge_refreeze` over `H46`, `R48`, `H43`, `R46`, `R47`, `F21`, `F22`, and `P27` | validated as a docs-only packet; preserves `H43` as the paper-grade endpoint, preserves `H46` as the prior decision packet, freezes `R48` as narrow comparator-only support, restores `no_active_downstream_runtime_lane`, and leaves `merge_executed = false` |
| F23 | The post-`H47` useful-case line can continue only through one planning-only numeric-scaling bundle that fixes `R49` as the only admissible next runtime candidate while preserving the exact-versus-comparator boundary | `F23_post_h47_numeric_scaling_bundle` over `H47`, `R46`, `R47`, `R48`, `P35`, and the post-`H47` extension master plan | validated as a planning-only bundle; preserves `H47` as the active docs-only packet, preserves `H43` as the paper-grade endpoint, fixes `R49` as the only next runtime candidate, keeps exact `R46/R47` evidence decisive relative to comparator-only `R48`, keeps `F24` dormant, and leaves `F25/P36` as placeholders only |
| D2a | The fixed useful-case ladder generalizes narrowly inside the same declared restricted surface | `R46_origin_useful_case_surface_generalization_gate` over eight held-out in-surface variants spanning the fixed `R44` three-kernel ladder | validated narrowly on `8/8` held-out variants across `3/3` kernels; still bounded useful-case only and not arbitrary `C` or broader Wasm |
| D2b | One structured static-memory frontend can lower exactly onto the preserved useful-case kernels without widening the runtime claim ceiling | `R47_origin_restricted_frontend_translation_gate` over eight held-out restricted-frontend variants spanning the fixed `R44` three-kernel ladder | validated narrowly on `8/8` held-out variants across `3/3` kernels with `translation_identity_exact_count = 8`; still bounded useful-case only and not arbitrary `C`, broader Wasm, or a new runtime stack |
| D2c | A comparator-only dual-mode useful-case model lane can stay exact on the preserved useful-case contract without replacing exact evidence | `R48_origin_dual_mode_useful_case_model_gate` over `compiled_weight_executor` and `trainable_2d_executor` on the fixed `R47` useful-case contract, with held-out trainable evaluation on `histogram16_u8` | validated narrowly on `2/2` admitted modes across `8/8` preserved useful-case variants, with the trainable mode also exact on the explicit held-out `histogram16_u8` family (`3/3` variants); remains comparator-only, bounded to useful cases, and non-substitutive relative to exact `R46/R47` evidence |
| D0 | The first compiled frontend boundary is now a preserved historical endpoint rather than the active mainline | `H27_refreeze_after_r32_r33_same_endpoint_decision`, `H28_post_h27_origin_core_reanchor_packet`, and `H29_refreeze_after_r34_r35_origin_core_gate` | preserved as historical narrow boundary; same-endpoint recovery remains closed while active routing stays on Origin-core substrate gates |
| D1 | A higher-level compiled pipeline can target this substrate | Restricted Wasm-like or tiny C-like compiler path | out of scope for the current Origin-core refreeze; any scope lift still needs a later explicit packet after `R36` |
