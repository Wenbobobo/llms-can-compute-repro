# F2 Future Frontier Recheck Activation Matrix

Planning-only lane for any future frontier review. `F2` does not run widened
experiments. It records what must be true before any later frontier recheck can
be justified from the frozen same-endpoint evidence.

`F2` is now downstream of
`H25_refreeze_after_r30_r31_decision_packet`, which preserves
`H23_refreeze_after_r26_r27_r28` as the current frozen scientific state. It
must not bypass the currently justified downstream order
`R32 -> deferred R33 -> blocked R29/F3`, and it does not authorize any widened
execution, scope lift, or demo narrative by itself.
