# R38 Todo

- keep the admitted family fixed to one richer control/call surface only;
- keep the long-form row explicit as a boundary probe rather than a second
  admission;
- export verifier/spec, lowering, and free-running exactness separately;
- preserve exact trace and exact final state as separate criteria;
- reject any interpretation that widens the result into arbitrary `C`, broad
  compiler support, or same-endpoint recovery.
