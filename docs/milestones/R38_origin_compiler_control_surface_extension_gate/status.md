# R38 Status

- completed narrow compiled control-surface extension gate on the active
  Origin-core substrate;
- keeps the opcode surface fixed to the `R37` set;
- treats `subroutine_braid_program(6, base_address=80)` as the only admitted
  extension row;
- exports `subroutine_braid_long_program(12, base_address=160)` as a
  non-admission boundary probe;
- does not reopen same-endpoint systems recovery or broad compiler claims.
