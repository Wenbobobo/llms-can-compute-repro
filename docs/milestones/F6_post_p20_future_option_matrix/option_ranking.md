# Option Ranking

## Ranked near-term options

1. Continue docs-only maintenance and entrypoint synchronization
   - status: preferred default
   - why: this preserves the now-clean `H34/P20/F5` interpretation without
     changing scientific scope.
2. Continue planning-only future gating downstream of `H34`
   - status: admissible
   - why: future-stage prep is still useful if it stays explicitly
     non-executing.
3. Prepare a contradiction-driven same-substrate design only if one unique
   candidate emerges later
   - status: conditional only
   - why: `F5` rejected all currently available reopen stories.
4. Merge or promote into another branch if it becomes operationally worthwhile
   - status: optional
   - why: useful for hygiene, but not required for the scientific mainline.
5. Start a new runtime batch now
   - status: rejected
   - why: no active downstream runtime lane exists after `H34`.
