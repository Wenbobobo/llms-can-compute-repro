# Future Option Matrix

| Option class | Current admissibility | Preconditions | Why |
| --- | --- | --- | --- |
| Docs-only or publication-system maintenance | admissible now | keep packet state unchanged | `P20` cleaned the paper-facing bundle, but repo/docs hygiene can continue without reopening science |
| Planning-only future gating or frontier preparation | admissible now as planning-only | no execution by default | later agents may still refine future gating surfaces downstream of `H34/F5` |
| Contradiction-driven same-substrate reopen | not admissible now | one unique contradiction must be named under the `F5` admissibility rules | `F5` concluded `no_reopen_candidate_survives` on the present evidence |
| Broader scope lift, new substrate, Wasm-like or arbitrary-`C` widening | blocked | later explicit scope decision plus stronger scientific evidence | current packets still reject widening by momentum |
| Merge or promotion into another branch | operationally optional | target branch and integration path should be clean enough to make sense | this can matter for hygiene, but it does not change scientific routing |

## Default

The current default is:

- `hold_no_reopen_freeze_and_continue_docs_or_planning_only_work`

That means the absence of a runtime lane is not a bug. It is the current
selected scientific state.
