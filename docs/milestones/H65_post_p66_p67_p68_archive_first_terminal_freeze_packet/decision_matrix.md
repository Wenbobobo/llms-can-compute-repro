# Decision Matrix

- selected current route:
  `archive_first_terminal_freeze_becomes_current_active_route_and_defaults_to_explicit_stop`
- default downstream state: `explicit_archive_stop_or_hygiene_only`
- only conditional later gate:
  `r63_post_h62_coprocessor_eligibility_profile_gate`
- any later `R63` discussion remains non-runtime and advisory-only unless a
  later explicit packet says otherwise
