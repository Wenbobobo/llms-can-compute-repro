# TODO

- keep `H64` explicit as the preserved prior active packet
- keep `R63` dormant and non-runtime only
- keep explicit archive stop or hygiene-only follow-through as the default
  downstream state
