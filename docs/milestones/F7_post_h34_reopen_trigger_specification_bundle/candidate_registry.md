# Candidate Registry

Current registry state: no active reopen candidate is registered.

This registry exists so future agents can record candidate contradictions
without silently upgrading them into runtime authorization.

| Candidate id | Trigger family | Admissibility class | Current status | Why it is not active now |
| --- | --- | --- | --- | --- |
| `same_row_exactness_break_on_admitted_or_boundary_probe` | exactness/final-state contradiction on current rows | admissible in principle | inactive | not observed on the current `R37/R38/R39` rows |
| `sharper_same_row_control_surface_dependence` | stronger same-row dependence than the declared `R39` permutation | admissible in principle | inactive | no unique perturbation currently dominates strongly enough to justify a reopen |
| `runtime_irrelevance_via_compiler_helper_overencoding` | sharper "compiler did all the work" contradiction on the current rows | admissible in principle | inactive | concern preserved, but no single decisive comparator is isolated yet |
| `broader_compiled_family_or_language_widening` | new family / new opcode surface / Wasm-like / arbitrary `C` pressure | inadmissible from current state | blocked | scope lift, not same-substrate contradiction |
| `historical_same_endpoint_systems_revisit` | revisit old `H21/H27` systems dissatisfaction | inadmissible from current state | blocked | preserved historical route only; not the active Origin-core line |
| `frontier_or_demo_pressure` | headline, demo, or communication dissatisfaction | inadmissible from current state | blocked | narrative pressure is not scientific contradiction |

## Registry rule

The registry should remain empty of active candidates until one row above
changes from `inactive` to `proposed` via a new explicit contradiction design.
That design must satisfy the `F5` admissibility rules and the `F7`
reopen-packet template before any execution becomes discussable.

## Current saved verdict

- `no_reopen_candidate_survives`
- `no active downstream runtime lane`
