# Acceptance

- one explicit post-`R32` same-endpoint refreeze is exported;
- `H26` states whether the boundary story moved materially or remained bounded
  without localization;
- one downstream routing decision is explicit:
  preserve deferred `R33`, stop same-endpoint follow-up, or require a later
  explicit packet before new runtime;
- `R29` and `F3` stay blocked unless reauthorized later.
