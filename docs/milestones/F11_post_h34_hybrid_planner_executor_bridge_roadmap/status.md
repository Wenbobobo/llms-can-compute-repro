# F11 Status

- inactive roadmap surface only;
- family classification: `requires_new_substrate`;
- the completed `F10` bridge plus current `F13` bounded-family spec are still
  insufficient to activate this family because planner/executor interface
  semantics sit outside the current substrate;
- current repo evidence does not yet support a hybrid planner-executor bridge;
- any later activation would need a new substrate story plus later explicit
  scope decisions.
