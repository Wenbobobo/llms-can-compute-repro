# Acceptance

- one bounded family-local sharp zoom manifest is explicit before execution;
- one ceiling-relative first-pass ladder is explicit before execution;
- one first-fail / no-localization verdict vocabulary is fixed before
  execution;
- one required output set is explicit before execution;
- `R32` is still recorded as the primary authorized next science lane;
- no fifth global axis or wider endpoint is introduced.
