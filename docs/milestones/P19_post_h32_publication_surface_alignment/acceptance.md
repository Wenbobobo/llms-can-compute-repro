# P19 Acceptance

- paper-facing entrypoints no longer present `H25/H23` as the current mainline;
- `H32` remains the explicit current active packet;
- historical same-endpoint control packets remain available but clearly marked
  as preserved context;
- no runtime or result artifact is changed here.
