# P19 Post-H32 Publication Surface Alignment

Docs-only publication/control alignment after the saved post-`H32` planning
surface.

`P19` exists to remove remaining paper-facing wording that still treats
`H25/H23` as current rather than preserved historical same-endpoint context.

This lane does not change the active scientific stage. It only aligns
publication-facing entrypoints with the already landed `H32` driver state.
