# F27 Todo

- [x] if and only if `H50` is positive, define one bounded entry bundle for
  either a trainable-executor route or a transformed/compiled-executor route;
- [x] keep the route bounded, falsification-first, and downstream of
  `R51/R52/H50`;
- [x] do not reopen broader Wasm, arbitrary `C`, or headline-level claims by
  momentum; and
- [x] preserve the bundle as non-selected after landed negative `H50`.
