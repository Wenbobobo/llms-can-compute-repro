# F27 Entry Scope

If some later explicit packet overturns the current negative `H50`, `F27` may
define exactly one bounded next planning surface:

- one trainable-executor entry route; or
- one transformed/compiled-executor entry route.

It may not define both at once without a later explicit packet, and it remains
inactive on the landed branch.
