# Candidate Matrix

Only candidates already named in `F14` are allowed here.

| Candidate id | Target threat | Allowed rows | Must preserve | Primary observable |
| --- | --- | --- | --- | --- |
| `helper_annotation_ablation_or_canonicalization` | `runtime_irrelevance_via_compiler_helper_overencoding` | both `R40` rows | source meaning, family, opcode surface, lowered semantics | whether helper-neutralization changes free-running exactness or workload shape |
| `control_surface_neutralization_without_semantic_change` | `runtime_irrelevance_via_compiler_helper_overencoding` | both `R40` rows | source meaning, family, opcode surface, workload role, lowered semantics | whether one declared control-surface choice is carrying the positive result |
| `retrieval_critical_vs_local_easy_step_contrast_slicing` | `fast_path_only_helps_the_easy_part` | both `R40` rows, but only with one predeclared slice pair per row | row identity, source meaning, opcode surface | whether observed gain sits only on easy spans while retrieval-critical work remains hard |

No additional candidate may be added inside `R41`.
