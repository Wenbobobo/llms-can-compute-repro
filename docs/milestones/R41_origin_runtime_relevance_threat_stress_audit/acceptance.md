# Acceptance

- `R41` remains saved as a deferred future design only.
- The audit scope stays fixed to the two landed `R40` positive rows.
- The only active threat families remain
  `runtime_irrelevance_via_compiler_helper_overencoding` and
  `fast_path_only_helps_the_easy_part`.
- The comparator and measurement rules are fixed before any future execution.
- No new family, opcode, substrate, restricted-Wasm lane, frontier lane, or
  hybrid runtime lane is authorized by this milestone.
