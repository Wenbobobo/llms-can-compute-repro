# Status

Not started. Still intentionally blocked.

Current blocker summary:

- the neural branch is exact only in a narrow structured-label form,
- precision decomposition is promising but still limited,
- the standard baseline still has zero exact rollout.
