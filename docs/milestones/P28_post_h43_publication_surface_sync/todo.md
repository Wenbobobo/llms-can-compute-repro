# P28 Todo

- sync publication-facing ledgers to the landed `H43/H42/H41/H36` stack;
- stop describing `R43` and `R44` as deferred in review/release/submission
  helpers;
- keep `R41`, `R29`, `F3`, arbitrary `C`, and merge-to-`main` blocked here;
- record the synced surface set in one machine-readable export packet; and
- preserve `H43` as the active scientific stage rather than replacing it with
  a docs-only publication lane.
