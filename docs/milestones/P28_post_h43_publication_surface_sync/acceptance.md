# P28 Acceptance

- the packet remains operational/docs-only;
- `H43` remains the current active scientific stage;
- publication-facing ledgers describe `R43`, `R44`, and `R45` as completed
  current gates rather than deferred future lanes;
- `P27` remains explicit with `merge_executed = false`; and
- no active downstream runtime lane is created after `H43`.
