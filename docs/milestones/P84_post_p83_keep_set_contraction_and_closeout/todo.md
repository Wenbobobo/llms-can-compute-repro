# TODO

- optional follow-through is limited to clean promotion/PR finalization from
  `wip/p83-post-p82-promotion-branch-and-pr-handoff` or explicit no further
  action;
- do not reopen runtime, same-lane executor-value work, broad Wasm,
  arbitrary `C`, transformed/trainable entry, or dirty-root integration; and
- keep `p81` and `p82` preserved as unmounted lineage unless a future audit
  requires historical reinspection.
