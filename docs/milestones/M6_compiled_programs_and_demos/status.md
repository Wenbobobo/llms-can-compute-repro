# Status

Not started. Still intentionally blocked.

Current blocker summary:

- `M4` now has exact and induced structured executors, but not yet a neural
  token/event decoder branch.
- `M5` now has a runnable CUDA baseline, but its first exact free-running
  rollout result is still zero on the current eval slice.
