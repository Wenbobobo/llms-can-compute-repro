# Blocked Hypotheses

- "A compiled demo should come next."
  Blocked because `M5` still lacks any exact free-running baseline success and
  `M4` still lacks a neural event decoder branch.
- "Sudoku or Hungarian would currently sharpen the scientific claim."
  Blocked because they would add surface complexity before the current
  execution, precision, and baseline questions are settled.
