# Brainstorm Log

- `M6` should start from the narrowest frontend that still stresses the core
  execution substrate. A tiny bytecode or Wasm-like subset remains the best
  default.
- The current `M5` result is a reminder that demos should wait for cleaner
  rollout behavior. Otherwise `M6` risks becoming a packaging layer over weak
  internals.
