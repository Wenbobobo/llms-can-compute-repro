# H37 Todo

- [x] Keep the two-outcome fork explicit:
      `keep_h36_freeze` versus
      `authorize_r41_origin_runtime_relevance_threat_stress_audit`.
- [x] Keep `H36` explicit as the preserved prior active routing/refreeze
      packet throughout.
- [x] Make the default-decision basis explicit:
      no uniquely isolated admissible candidate currently survives on the fixed
      landed `R40` row pair.
- [x] Keep `R41`, `R29`, `F3`, and `F2` non-active by default.
- [x] Require a later explicit packet before any future same-substrate reopen.
