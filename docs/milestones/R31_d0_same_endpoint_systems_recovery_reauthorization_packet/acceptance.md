# Acceptance

- one explicit verdict:
  `hold_r29_blocked`,
  `audit_non_retrieval_overhead_first`, or
  `execute_one_bounded_same_endpoint_recovery_probe`;
- one explicit dominant bottleneck hypothesis;
- one explicit comparator set;
- one explicit recommended next lane, if any;
- no scope drift beyond the current positive `D0` suite.
