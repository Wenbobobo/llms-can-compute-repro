# F4 Post-H23 Origin Claim Delta Matrix

Planning-only origin-facing delta matrix.

`F4` exists so later stages can measure progress against the origin article and
the three discussion files without rereading the entire repo or drifting back
to headline framing. It does not authorize execution or public-surface
expansion.

The matrix stays anchored to the frozen `H23` scientific evidence while
remaining operationally downstream of `H25`.
