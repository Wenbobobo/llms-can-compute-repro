# Merge-Main Planning Constraints

- `main` may be discussed only as a later planning target, never as the live
  execution surface for this wave.
- any later merge-main planning must start from a promoted clean descendant,
  not from the parked root worktree.
- `merge_executed = false` remains mandatory throughout this packet.
