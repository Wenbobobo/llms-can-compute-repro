# P24 Acceptance

- `docs/publication_record/current_stage_driver.md` names `H36` as the current
  active stage;
- `tmp/active_wave_plan.md` names `H36` as the current active routing packet;
- the milestone and plans indexes point to `H35`, `R40`, `H36`, and `P24`;
- the synced surfaces make clear that no active downstream runtime lane exists
  after `H36`.
