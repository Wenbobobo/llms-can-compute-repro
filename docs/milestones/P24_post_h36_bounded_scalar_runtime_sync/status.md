# P24 Status

- completed docs-only control-surface sync after the landed `H35/R40/H36`
  wave;
- records `H36` as the current active routing packet;
- records `H35` as the preserved prior docs-only runtime-decision packet;
- records `R40` as completed bounded-scalar same-substrate evidence;
- keeps `no active downstream runtime lane` explicit after `H36`.
