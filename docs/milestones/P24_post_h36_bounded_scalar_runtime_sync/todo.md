# P24 Todo

- refresh the driver and active-wave handoff to the landed `H35/R40/H36`
  state;
- refresh milestone and plans indexes to point at the new bounded-scalar
  runtime wave;
- keep `R41_origin_runtime_relevance_threat_stress_audit` deferred only;
- keep broader frontier, restricted-Wasm, same-endpoint, and hybrid lanes
  blocked or inactive by default.
