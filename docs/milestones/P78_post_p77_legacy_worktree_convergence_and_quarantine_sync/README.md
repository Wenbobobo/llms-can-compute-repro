# P78 Post-P77 Legacy Worktree Convergence And Quarantine Sync

Convergence wave that removes clean legacy mounts, preserves the balanced keep
set, and leaves only explicit dirty quarantines mounted.
