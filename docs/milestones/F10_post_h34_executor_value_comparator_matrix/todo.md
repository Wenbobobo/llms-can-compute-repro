# Todo

- [ ] If activated, enumerate richer executor-visible value classes and the
  invariants they would need to preserve against the current Origin-core line.
- [ ] Keep the matrix comparator-only and explicitly downstream of
  `H34/F5/F6`.
- [ ] Refuse any activation that quietly turns comparator design into a runtime
  lane.
