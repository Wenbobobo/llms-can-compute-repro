# Todo

- [x] Distill the hardest surviving `R21/R22` branches into one prioritized
  candidate core for any future reopen.
- [x] Define a zoom-in matrix around the most plausible near-boundary axes and
  the current maxima that any later run would have to exceed.
- [x] Specify deterministic seeds, first-fail artifacts, and stop rules before
  any execution is authorized.
- [x] Predeclare the future verdict vocabulary so a later run can end cleanly
  without an open-ended repair loop.
- [x] Record blocked hypotheses that must not be smuggled into a later reopen.
- [x] Keep this lane planning-only until a later explicit reopen plan says
  otherwise.
