# Research Notes

- The current label space is almost entirely opcode-stable, so a neural rule
  table is the right first learned target.
- For this phase, exact event semantics matter more than raw sequence
  generation.
- This milestone should be read as "neuralizing the induced executor," not as a
  generic neural program executor.
