# Brainstorm Log

- The first learned branch should use the induced label space instead of whole
  event tokens.
- Context features were initially too flexible and hurt generalization.
- An opcode-conditioned rule decoder is a better checkpoint than a fragile
  pseudo-token model.
