# Blocked Hypotheses

- The current result does not show that a token LM can execute traces exactly.
- The current result does not show that richer context dependence is already
  solved.
- The current result does not settle how far the learned branch scales under
  finite precision.
