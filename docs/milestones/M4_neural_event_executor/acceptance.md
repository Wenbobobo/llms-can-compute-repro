# Acceptance

- The learned branch must run online without exact opcode fallback.
- Held-out evaluation must report exact trace and exact final state.
- At least one held-out memory family must remain exact, not only countdowns.
- Any limitation versus token-level decoding must be called out explicitly.
