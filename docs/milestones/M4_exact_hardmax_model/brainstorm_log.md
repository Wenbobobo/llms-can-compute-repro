# Brainstorm Log

- The induced causal executor is intentionally synthesis-first. This keeps the
  branch aligned with the Discuss files, which repeatedly warn against taking a
  weak teacher-forced model as evidence for exact execution.
- The next neural `M4` move should not copy the current `M5` token pipeline
  blindly. `M4` still benefits from structure, so an event-level decoder is a
  better next target than a flat token LM.
- Precision work remains co-equal with learning work. A stronger learned
  decoder on top of an unstable address encoding would not strengthen the core
  scientific claim.
