# H32 Status

- current active routing/refreeze packet after `H31` and `R38`;
- preserves `H30` as the prior narrow compiled-boundary refreeze packet;
- freezes one richer compiled control/call family as narrow local evidence on
  the same substrate;
- keeps `R29`, `F3`, and broader headline claims blocked;
- completes clean-worktree packaging in
  `P18_post_h32_clean_worktree_promotion`.
