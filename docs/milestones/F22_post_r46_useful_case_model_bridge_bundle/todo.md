# F22 Todo

- keep `F22` saved only as a blocked future comparator bundle after `H45`;
- keep exact `R47` explicit as the gating prerequisite before any model-side
  useful-case comparator work can become admissible;
- keep later explicit `H46` explicit as the required interpretation packet
  before `R48` can be authorized;
- keep `compiled_weight_executor` and `trainable_2d_executor` subordinate to
  the landed exact useful-case contract rather than substituting for it; and
- keep broader Wasm/C, hybrid model work, and merge-to-`main` non-active here.
