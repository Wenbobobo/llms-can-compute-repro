# R44 Origin Restricted Wasm Useful-Case Execution Gate

Completed restricted-Wasm / tiny-`C` useful-case gate authorized by completed
`H42`.

`R44` now stores the landed smallest useful-case gate that validates the fixed
restricted kernel ladder on the same append-only substrate after bounded
retrieval and bounded-memory VM execution survived. The completed gate keeps
the claim ceiling narrow: bounded useful kernels only, with no arbitrary `C`,
no unrestricted Wasm, and no model lane replacing exact evidence.

This milestone preserves:

- `H42` as the completed upstream docs-only route-selection packet that
  authorized `R44`;
- `H36` as the preserved active routing/refreeze packet;
- `F19` as the fixed semantic-boundary useful-case roadmap;
- `R43` as the decisive upstream exact bounded-memory execution gate;
- `R45` as a coequal comparator lane that does not replace exact evidence;
- `P27` as the explicit merge packet with `merge_executed = false`; and
- the fixed three-kernel useful-case ladder without broader surface lift.

The landed fixed-order kernels are:

1. `sum_i32_buffer`
2. `count_nonzero_i32_buffer`
3. `histogram16_u8`
