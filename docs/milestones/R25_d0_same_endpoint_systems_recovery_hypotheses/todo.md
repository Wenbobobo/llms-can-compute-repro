# Todo

- [ ] Decompose the `R23` mixed verdict into concrete same-endpoint bottleneck
  hypotheses, including fixed overhead, retrieval-total concentration,
  comparator-path gaps, and other bounded runtime costs.
- [ ] Separate hypotheses that stay scientifically inside the current endpoint
  from ideas that would secretly widen scope.
- [ ] Predefine what evidence would count as `systems_materially_positive`,
  `systems_still_mixed`, or a stronger negative same-endpoint outcome.
- [ ] Record negative signals that should kill a future systems-recovery reopen
  early instead of starting a long repair loop.
- [ ] Keep this lane parked until `P12` is closed out and any later explicit
  reopen plan says otherwise.
