# Acceptance

- `R58` remains a narrow runtime gate on a fixed typed stack-bytecode suite.
- The five admitted row categories remain fixed and explicit.
- Exact normalized source-vs-spec parity is reported on every row.
- Exact source-vs-lowered trace and final-state parity are reported on every
  row.
- First-fail localization is exported.
- `R59` becomes the only next runtime candidate after positive exact `R58`.
