# Comparator Matrix

- transparent source bytecode interpreter execution
- transparent lowered trace interpreter execution
- free-running exact linear trace-VM execution
- free-running exact accelerated trace-VM execution

The comparator remains exactness-first. Timing is exported, but no bounded
fast-path value outcome is authorized here.
