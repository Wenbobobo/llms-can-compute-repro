# Execution Manifest

`R59` reuses the exact admitted `R58` rows only. For each row it records:

- source bytecode interpreter execution;
- lowered trace interpreter execution;
- free-running exact linear execution;
- free-running exact accelerated execution; and
- timing plus first-fail localization.
