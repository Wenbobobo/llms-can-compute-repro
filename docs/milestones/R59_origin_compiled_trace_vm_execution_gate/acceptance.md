# Acceptance

- `R59` remains a narrow execution gate on the exact `R58` rows only.
- Exact source-vs-lowered trace and final-state parity are visible.
- Exact linear and exact accelerated free-running parity are visible.
- First-fail localization is exported.
- Timing is exported without widening into a bounded fast-path value claim.
- `H54` becomes the only next packet after `R59`.
