# TODO

- keep the review scoped to docs/export/control/release surfaces
- allow promotion only if the exact `p63..p64` delta remains honest and narrow
- route any failed review directly to explicit archive stop rather than
  speculative continuation
