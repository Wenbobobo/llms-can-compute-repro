# P27 Acceptance

- the packet remains operational rather than scientific;
- the clean source branch and explicit merge branch are fixed separately;
- the merge set is explicit and reviewable;
- dirty historical work remains isolated from the promotion decision;
- `R43` stays the next exact gate and `R45` stays coequal but downstream of the
  exact evidence rule;
- `merge_executed` remains false inside `P27`.
