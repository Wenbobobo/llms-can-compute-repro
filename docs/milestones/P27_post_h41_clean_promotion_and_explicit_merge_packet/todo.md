# P27 Todo

- inventory the clean post-`H41` branches and worktrees;
- classify the merge set separately from unrelated dirty-tree history;
- decide push/merge readiness explicitly rather than by momentum.
