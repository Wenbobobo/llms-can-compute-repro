# P27 Todo

- inventory the clean post-`H41` source branch and explicit merge worktree;
- classify the reviewable merge set separately from unrelated dirty-tree
  history and oversized probes;
- keep `main` untouched until one later explicit manual merge review occurs;
- keep `R43` and `R45` execution in their own downstream worktrees.
