# Todo

- [x] Export the exact `R26` manifest before execution.
- [x] Run the declared `22`-candidate batch: `5` core anchors plus `6`
  first-wave extended branches with `2` seeds each.
- [x] Preserve `boundary_rows`, `branch_summary`, `first_fail_digest`, and
  `neighbor_exact_rows`.
- [x] End with one predeclared verdict instead of open-ended widening.
- [x] Trigger `R27` only if `R26` ends ambiguous or still fully exact.
