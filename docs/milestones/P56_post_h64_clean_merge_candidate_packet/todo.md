# P56 TODO

- keep merge-candidate prep separate from merge execution;
- keep dirty root `main` quarantine explicit and out of any integration path;
- keep raw-row ignore rules and zero tracked `>=10 MiB` artifacts as part of
  the packet contract;
- keep the later sidecars `P57`, `P58`, and `P59` operational rather than
  scientific.
