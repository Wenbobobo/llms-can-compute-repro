# P81 Post-P80 Locked-Fact Rebaseline And Route Sync

Sidecar that rebaselines the locked facts after `P80`, records the published
`p75` head, and keeps the default route at explicit stop plus clean-descendant
promotion-prep only.
