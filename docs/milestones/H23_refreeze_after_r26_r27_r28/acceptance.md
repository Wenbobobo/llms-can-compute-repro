# Acceptance

- one machine-readable refreeze packet lands with `summary.json`,
  `checklist.json`, `snapshot.json`, and `claim_packet.json`;
- `boundary_verdict`, `mechanism_contract_verdict`, and `systems_verdict` are
  all explicit;
- unsupported claims and unsupported broader claims remain explicit;
- the next downstream lane is recorded before any outward sync starts.
