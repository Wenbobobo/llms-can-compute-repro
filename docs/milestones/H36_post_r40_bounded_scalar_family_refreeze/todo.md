# H36 Todo

- freeze `H35` and `R40` into one explicit machine-readable packet;
- keep the supported, unsupported, and disconfirmed claims explicit;
- preserve `R29`, `F3`, `F9`, `F11`, and broader rhetoric as blocked or
  deferred;
- record that `R41_origin_runtime_relevance_threat_stress_audit` is only a
  deferred future candidate after a new explicit packet;
- hand control-surface updates to `P24_post_h36_bounded_scalar_runtime_sync`.
