# Acceptance

- `R27` executes only when `R26` explicitly triggers it;
- only one allowed mode runs;
- the lane remains on the current same-endpoint `D0` boundary;
- the lane ends with a bounded verdict rather than another undefined follow-up.
