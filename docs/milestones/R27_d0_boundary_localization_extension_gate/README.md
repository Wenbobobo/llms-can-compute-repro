# R27 D0 Boundary Localization Extension Gate

Conditional-only companion to `R26`.

`R27` exists for exactly two situations:

- `R26` found a near-boundary signal that needs confirmation; or
- `R26` stayed exact on the entire first wave and therefore justifies one
  predeclared second-wave extension.
