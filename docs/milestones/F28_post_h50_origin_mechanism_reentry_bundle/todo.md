# F28 Todo

- save the dedicated post-`H50` mechanism reentry master plan;
- define one mechanism claim-delta matrix over the active bounded evidence
  stack;
- define one reentry question that prefers mechanism falsification over
  transformed or trainable entry;
- define one route-constraints note that fixes `H51 -> R55 -> R56 -> R57 ->
  H52`;
- keep `R55` as the only next runtime candidate and `R56/R57` as conditional
  downstream gates; and
- keep broader Wasm, arbitrary `C`, transformed-model entry, trainable entry,
  and demo-first widening out of scope unless a later explicit packet changes
  the boundary.
