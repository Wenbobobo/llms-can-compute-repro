# Acceptance

- `claim_delta_matrix.md` maps the origin-facing claims onto the current
  `H34/F10/P22` state rather than an older frozen packet.
- `scientific_goal_stack.md` separates supported, narrowed, planning-only,
  blocked, and new-substrate ambitions.
- `repro_gap_ladder.md` ranks the next scientific gaps without creating a new
  runtime authorization.
- `origin_material_index.md` keeps the local-only origin inputs visible without
  copying restricted source material into the tracked repo.
