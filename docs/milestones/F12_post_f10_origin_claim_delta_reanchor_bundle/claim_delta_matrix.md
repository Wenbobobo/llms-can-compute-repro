# Claim Delta Matrix

| Origin-facing claim | Current status | Why |
| --- | --- | --- |
| Append-only execution traces can encode deterministic computation | `supported_here` | `H28 -> R34/R35 -> H29` preserves this as the current Origin-core scientific floor. |
| Exact `2D` hard-max retrieval can implement the key bounded read primitives | `supported_here` | `R34` preserves the narrow retrieval primitive contract on the active bundle. |
| Latest-write / stack-like retrieval can support a small exact executor | `supported_here` | `R35` validates a small exact stack/VM-style executor on the active narrow bundle and `H29` freezes that chain positively. |
| One tiny lowered/compiled subset plus one richer same-opcode control/call family can target the current substrate exactly | `supported_here` | `R37/R38/R39/H32/H34` preserve a narrow compiled-boundary line without widening it into a broad compiler claim. |
| The blog demos imply that larger flashy end tasks should drive the active mainline | `narrowed_here` | the repo preserves the mechanistic substrate as the active scientific target and treats demo pressure as non-authorizing. |
| The true executor boundary has already been localized cleanly | `narrowed_here` | the repo now preserves strong mechanism evidence but still does not claim clean boundary localization. |
| The next scientifically honest step is one bounded richer same-substrate value family | `planning_only` | `F10` identifies `bounded scalar locals and flags` as the nearest admissible richer family, but only at planning granularity so far. |
| The compiler or helper annotations may still be doing too much of the real work | `planning_only` | this remains a standing scientific risk and is the main reason `F14` prepares a contradiction-readiness surface without activating runtime. |
| Restricted Wasm-like or richer semantic-boundary work is already justified | `blocked_by_scope` | `F9` remains blocked and still requires a later explicit scope decision plus stronger evidence. |
| Arbitrary `C` has been reproduced | `blocked_by_scope` | current evidence stays on a narrow substrate and small compiled boundary only. |
| A general LLM has become a computer | `blocked_by_scope` | the repo continues to reject the headline framing as the current claim. |
| Million-step economics or broad systems competitiveness have been established | `requires_new_substrate` | the current evidence bundle is too narrow and still lacks a broader runtime, systems, and scope story. |
| A hybrid planner-executor system is the next same-substrate step | `requires_new_substrate` | `F11` remains explicitly new-substrate and cannot be smuggled in as a richer value family. |
