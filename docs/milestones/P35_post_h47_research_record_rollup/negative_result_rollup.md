# P35 Negative Result Rollup

## Preserved Negative Or Narrow Results

- `H27` closes the old same-endpoint recovery line negatively.
- `R23` keeps the same-endpoint systems verdict mixed rather than overturned.
- `R36` sharpens a real finite-precision boundary rather than supporting broad
  robustness.
- `H43` freezes useful-case support narrowly rather than authorizing broad
  `wasm/C`.
- `R48` is positive only as comparator support and does not replace exact
  evidence.
- `H47` explicitly restores `no_active_downstream_runtime_lane` rather than
  continuing by momentum.

## Practical Reading

The strongest remaining near-term question is now narrow numeric scaling on the
already-admitted useful-case surface. If that fails, the clean extension story
toward restricted `tiny-C` should stop rather than broaden.
