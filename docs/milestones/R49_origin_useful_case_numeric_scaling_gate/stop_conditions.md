# R49 Stop Conditions

- stop scientific success immediately if any widened case breaks verifier,
  scope, source/spec agreement, lowering parity, or free-running exactness;
- stop interpretation positively if `bucket_a` loses all admitted float32
  recovery regimes;
- stop transition to `F25` if `bucket_b` loses all admitted float32 recovery
  regimes, even when `bucket_a` still passes;
- do not activate `segment_rescaled`, `F24`, heap, recursion, IO, or broader
  compiler/runtime rhetoric to rescue a failed bucket; and
- if all three buckets stay exact with at least one admitted float32 recovery
  regime, stop at `H48` rather than widening by momentum.
