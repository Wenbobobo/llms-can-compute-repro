# Acceptance

- `R26`, `R27`, `R28`, and `H23` all have actionable milestone docs;
- `R26` manifest and `R27` trigger conditions are explicit before execution;
- `R28` is bounded to mechanism-contract auditing on the current `D0`
  evidence;
- no file in this stage silently opens a widened runtime or systems-repair
  lane.
