# Acceptance

- one explicit post-`R33` same-endpoint decision packet is exported;
- the packet keeps the endpoint fixed and the systems conclusion explicit;
- blocked future lanes are either preserved or explicitly reauthorized by a new
  decision, not by momentum;
- `H27` does not widen scope by itself.
