# F3 Post-H23 Scope-Lift Decision Bundle

Blocked future decision bundle.

`F3` remains planning-only after `H25`.

`F3` exists so a later agent can decide whether scope lift is even discussable
without rereading the entire repo history, but the current refreeze preserved
three explicit unsatisfied activation conditions and therefore does not
reauthorize any broader thesis, endpoint lift, or headline expansion.

`H25_refreeze_after_r30_r31_decision_packet` keeps
`H23_refreeze_after_r26_r27_r28` as the frozen scientific state, authorizes
only one future `R32` sharp-zoom lane plus deferred `R33`, and still leaves
`R29` and `F3` blocked.
