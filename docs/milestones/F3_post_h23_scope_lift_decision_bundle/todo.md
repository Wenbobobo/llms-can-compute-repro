# Todo

- [x] Read `H23`.
- [x] Read `H25`, `R30`, and `R31` as the current post-`H23` routing packet.
- [x] Record the remaining unsatisfied frontier-activation conditions:
  `true_executor_boundary_localization`,
  `current_scope_systems_story_materially_positive`, and
  `scope_lift_thesis_explicitly_reauthorized`.
- [x] Record that `R32` is the primary next science lane, `R33` is the
  deferred systems-audit lane, and `R29` stays blocked.
- [x] Keep this bundle planning-only unless a later refreeze explicitly
  reauthorizes scope lift.
