# Acceptance

- `docs/publication_record/current_stage_driver.md` still states that `H32` is
  active, `H34` is the current docs-only control packet, and there is no active
  downstream runtime lane.
- the driver now also points to `F7` and `F8` as the current planning-only
  follow-on surfaces and does not describe them as runtime authorization.
- `tmp/active_wave_plan.md` matches that same planning-only reading.
- `docs/milestones/README.md`, `docs/plans/README.md`, `README.md`, and
  `STATUS.md` point to the new planning-only surfaces without implying reopen.
