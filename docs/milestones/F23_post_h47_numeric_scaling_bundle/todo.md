# F23 Todo

- preserve `H47`, `H43`, `R46`, `R47`, and `R48` explicitly in the bundle;
- define one allowed precision regime matrix for `R49`;
- define one allowed addressability strategy table for `R49`;
- define one explicit length-bucket plan for `R49`;
- define one explicit kill-criteria sheet for early stop;
- keep `R49` as the only next runtime candidate;
- keep `F24` dormant and non-authorized;
- scaffold `F25` as the positive downstream placeholder only; and
- scaffold `P36` as the negative closeout placeholder only.
