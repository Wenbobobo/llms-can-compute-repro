# Experiment Matrix

- Inputs: stabilized `results/M6_typed_bytecode_harness/`,
  `results/M6_memory_surface_followup/`, synchronized `P1_paper_readiness`
  ledgers, and the Python 3.12 + `uv` scripts that export figures.
- Interventions: rerun export/render scripts, update root `README.md` and root
  `STATUS.md`, and catalog public-safe vs restricted artifacts plus blocked
  blog triggers.
- Outputs: refreshed tables/figures, the packaging ledger inside this
  milestone, and a structured stop/go gate for any future blog or compiled-demo
  draft.
