# Brainstorm Log

- Leverage the `docs/milestones/M6_*` ledgers as inputs for the paper narrative; each milestone should contribute one section that clearly states the input claim and its verification status.
- Use parallel worktrees or subagents to keep packaging work isolated from the main `src/` changes, letting the public copy stay stable while instrumentation continues.
- Store unreleased blog drafts here but flag them as blocked until the claim ladder records a stable `C3e` boundary.
