# P2 Public Research Packaging

Goal: curate the evidence, artifacts, and narratives needed for a disciplined public presentation (paper-first, ledger-first) once `M6` control-flow instrumentation and its memory follow-up stabilize.

Scope:

- refresh root `STATUS.md` and `README.md` to highlight completed phases
  without hype, keeping tone restrained and science-forward;
- aggregate `results/*` exports, ledgers, and exact-trace artifacts required for `P1_paper_readiness`, while keeping the blog channel blocked until claims are airtight;
- align packaging with the developer-friendly Python 3.12 + `uv` stack so downstream reproducibility stays consistent with the main repo;
- provide machine-readable metadata, experiment summaries, and publication notes inside this milestone tree for future unattended phases.

Deliverables:

- a public-facing README/status explaining the current claim landscape;
- a `P1`+`P2` artifact ledger plus script stack to regenerate exports and figures;
- a public-safe vs restricted artifact ledger that keeps `docs/Origin/`
  explicitly out of public packaging while exposing the derived evidence;
- communication guidance (paper outline, blog hold policy, risk log) captured inside this directory before any outward publish.
