# Decision Log

- Paper readiness comes before any blog mention; blog entries stay in `blocked_hypotheses.md` until claims and ledgers are updated.
- Keep packaging updates readable and machine-actionable so future unattended phases can resume with minimal context.
- Tie every new artifact to the ledger entries produced by `scripts/export_p1_paper_readiness.py` and note the exact command used.
- Treat `results/M6_memory_surface_followup/` as a public-safe diagnostic
  companion to `D0`, not as justification for a broader frontend claim.
