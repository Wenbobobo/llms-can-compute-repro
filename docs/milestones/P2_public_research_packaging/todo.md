# Todo

- [x] Rerun `scripts/export_p1_paper_readiness.py` plus
  `scripts/render_p1_paper_artifacts.py` after the new `M6` exports and
  confirm the exact-trace table still remains `partial`.
- [x] Update the public README/STATUS to mention the memory-surface follow-up
  while keeping the tone restrained and documentation-style.
- [x] Draft a paper-first workflow in `research_notes.md` that references
  `D0`/`C2h`/`C3e` and spells out which experiments back each claim.
- [x] Capture a ledger of which artifacts are safe to share, which are
  restricted, and the reproduction commands for each (Python 3.12 + `uv`).
- [x] Log the blocked blog timing triggers in `blocked_hypotheses.md` so future
  releases know what still needs verifying.
- [x] Keep the release ledger synchronized if `M6-D/E` adds new public-safe
  artifacts or changes reproduction commands.
- [x] Keep `README.md`, `STATUS.md`, and `docs/publication_record/` aligned
  with the `P3` → `P4` gate chain once wording is locked.
- [x] Prepare a coherent release/commit split before the next outward sync so
  the public history remains reviewable.
