# Blocked Hypotheses

- Do not publish the blog until `results/M6_memory_surface_followup` ledger confirms that memory diagnostics do not widen the claim set.
- Do not publish a compiled-demo narrative until the current `D0` slice is
  explicitly frozen and survives one serious stress family plus one external
  reference comparison.
- Assume the current `P1` export scripts remain sufficient; revisit only if new
  artifacts require extra formatting.
- Treat any claim that lacks an exact-trace or exact-final-state row as unresolved and therefore blocked.
