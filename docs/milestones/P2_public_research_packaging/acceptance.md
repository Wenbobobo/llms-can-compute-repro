# Acceptance

- The `P1` export scripts and rendered tables/figures are rerun with the latest M6 data and recorded alongside new packaging metadata.
- README/STATUS describe the science first, leaving the blog entry off-limits until ledger entries confirm claim stability.
- A strict publishing ledger records which artifacts are public-safe, which
  remain restricted, and the minimal Python 3.12 + `uv` steps needed to
  reproduce each export.
- Packaged assets include machine-readable claims, experiment digests, and risk notes that can guide future unattended stretches.
