# R47 Status

- next authorized exact runtime candidate after completed `H45`;
- must translate one extremely restricted frontend surface onto the existing
  useful-case bytecode kernels and exactness pipeline;
- must preserve the landed bounded useful-case claim ceiling;
- remains below heap allocation, alias-heavy pointers, recursion, float, IO,
  hidden mutable state, and broad compiler/runtime claims;
- must route any interpretation through later explicit
  `H46_post_r47_frontend_bridge_decision_packet`;
- keeps `F22` blocked while exact frontend evidence is still unexecuted; and
- does not authorize `R48`, broader Wasm/C, or merge-to-`main`.
