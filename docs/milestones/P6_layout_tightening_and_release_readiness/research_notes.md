# Research Notes

- After `P5`, the manuscript is limited more by presentation discipline than by
  missing evidence.
- The mixed `R2` gate should remain visible and non-triumphal during layout
  tightening; presentation cleanup must not soften the no-go logic behind `M7`.
- `D0` remains an endpoint on current evidence, so layout work must not let
  companion tables read like hidden widening.
- Blog and broader release prose remain downstream and blocked unless a later
  phase changes the public-release gate explicitly.
