Near-term order:

`F32 -> H59 -> P41 -> P42 -> F33`

Allowed next action after this bundle:

- planning-only candidate screen or explicit project stop

Disallowed by default:

- another `R62`-like executor-value probe on the same runtime path
