Candidate families:

1. amortized/indexed retrieval over repeated trace queries
2. low-level compiled or external coprocessor retrieval kernels
3. transformed or trained executor routes only if they change the cost model
   materially and still target the narrow useful-case endpoint honestly
