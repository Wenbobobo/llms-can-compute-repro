# R50 Origin Restricted Tiny-C Lowering Gate

Authorized but not yet executed runtime candidate after completed `F25`.

`R50` is the only next runtime lane fixed by `F25`. It must test one
restricted tiny-`C` lowering route onto the preserved `R47` useful-case
contract first, using the same fixed `3/3` kernel ladder and the same held-out
`8/8` useful-case variants before any later packet considers broader wording
or fused numeric widening.

Current status: `planned_by_f25_not_yet_executed`.
