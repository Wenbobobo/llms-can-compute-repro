# Acceptance

- the clean successor branch is explicit and reviewable;
- packet split between scientific/runtime artifacts and current-facing doc
  closeout is fixed in writing;
- the focused gate-specific test suite and `git diff --check` run clean on the
  promotion lane;
- no dirty-tree merge is required to understand or verify the packet.
