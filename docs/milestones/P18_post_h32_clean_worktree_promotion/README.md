# P18 Post-H32 Clean Worktree Promotion

Immediate operational closeout lane after landed `H32`.

`P18` exists to keep the `H31/R38/H32` packet and its current-facing doc sweep
inside one clean successor worktree rather than reopening dirty `main` or dirty
`wip/h27-promotion`.

This lane is not a new science packet. It packages the landed narrow result and
the updated control docs for later review or merge.

Execution details are fixed in:

- `commit_split_manifest.md`
- `worktree_runbook.md`
