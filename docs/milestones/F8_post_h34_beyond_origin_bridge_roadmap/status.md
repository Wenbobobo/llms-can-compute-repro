# F8 Status

Initial planning scaffold saved downstream of `H34/P20/F5/F6`.

- this lane is planning-only;
- it preserves the current `H32`/`H34` no-reopen state;
- it maps beyond-Origin bridge families without activating any of them;
- it keeps richer compiled-family, Wasm-like, and hybrid planner-executor work
  explicitly non-active;
- only the current Origin-core line remains `supported_here`.
