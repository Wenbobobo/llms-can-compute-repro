# Origin To Repo Bridge

This note maps "beyond origin" ambitions onto the current repo without
relabeling them as current results.

`planning_only` below means docs-only classification or comparator work, not an
authorized runtime or implementation lane.

| Origin-facing ambition | Current repo anchor | Current status | Nearest admissible next form |
| --- | --- | --- | --- |
| Deterministic computation from append-only traces | `H28/R34/R35/H29` Origin-core packet | `supported_here` | preserve as the current scientific floor |
| Narrow compiled exactness on the same substrate | `R37/R38/R39/H32/H34` | `supported_here` | preserve as narrow compiled-boundary support only |
| Explain what a post-Origin bridge would need to preserve before any richer semantic frontend | `F8` roadmap framing | `planning_only` | docs-only target ladder and family matrix |
| Ask whether a restricted semantic frontend such as a Wasm-like boundary could ever be a justified next family | `F9` placeholder only | `blocked_by_scope` | a future explicit scope packet plus stronger evidence than the current narrow same-substrate bundle |
| Compare richer executor-visible value obligations before a frontend shift is attempted | `F10` placeholder only | `planning_only` | a docs-only comparator matrix kept downstream of `H34/F5/F6` |
| Let a broader planner route work into the exact executor substrate | `F11` placeholder only | `requires_new_substrate` | a genuinely new substrate or hybrid-system packet, not a same-substrate reopen |
| Claim arbitrary `C`, general LLM computation, or broad platform parity | no current active lane | `requires_new_substrate` | substantially broader runtime, systems, and scope evidence than the repo currently has |

The bridge value of `F8` is therefore classificatory, not operational. It keeps
future families visible without implying that any of them are currently active.
