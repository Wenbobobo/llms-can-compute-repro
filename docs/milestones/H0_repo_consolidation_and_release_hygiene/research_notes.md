# Research Notes

- `H0` exists to prevent operational mess from distorting scientific
  communication.
- The current repository already has a resolved scope decision: stay on the
  narrow `D0` endpoint and keep the blog blocked.
- A commit split is useful here because a large undifferentiated dirty tree
  makes future review and public release harder, even when the science itself
  is stable enough for the current checkpoint.
