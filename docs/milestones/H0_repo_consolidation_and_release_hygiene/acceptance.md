# Acceptance

- one machine-readable worktree audit exists;
- one machine-readable commit-split plan exists;
- one public-surface audit exists, covering `README.md`, `STATUS.md`,
  `.gitignore`, the `P2` artifact ledger, and the `M7` / `P4` outputs;
- milestone docs explain that release hygiene is an operational checkpoint, not
  a new scientific result.
