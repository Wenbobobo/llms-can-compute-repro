# P25 Post-H36 Clean Promotion Prep

Immediate operational promotion-prep lane after the landed
`H35 -> R40 -> H36 -> P24` wave.

`P25` exists to prepare one clean promotion inventory from the real
source-of-truth branch `wip/h35-r40-p24-exec` without touching dirty `main`.
It is not a new science lane and it does not authorize a merge by momentum.

Execution details are fixed in:

- `promotion_manifest.md`
- `hygiene_summary.md`
- `source_of_truth_delta.md`
- `worktree_runbook.md`
