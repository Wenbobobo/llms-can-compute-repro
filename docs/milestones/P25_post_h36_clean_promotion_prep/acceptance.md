# P25 Acceptance

- `promotion_manifest.md` names
  `wip/h35-r40-p24-exec` as the scientific source of truth and
  `wip/p25-f15-h37-exec` as the clean prep branch;
- `hygiene_summary.md` makes `prepare_only` explicit and keeps dirty `main`
  untouched;
- `source_of_truth_delta.md` groups the landed packet window ahead of `main`
  without relabeling it as merged;
- `worktree_runbook.md` records a future promotion procedure without
  authorizing it now.
