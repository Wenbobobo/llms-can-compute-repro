# R48 Todo

- [x] evaluate `compiled_weight_executor` and `trainable_2d_executor` on the
  preserved useful-case contract only;
- [x] keep the held-out useful-case family explicit and fixed as
  `histogram16_u8` rather than widening the evidence contract;
- [x] keep exact evidence decisive and non-substitutive relative to model
  positives;
- [x] export first-error position, failure class, exact row counts, and
  per-mode rollups; and
- [x] keep broader Wasm/C, broader hybrid model work, and merge-to-`main`
  non-active here.
