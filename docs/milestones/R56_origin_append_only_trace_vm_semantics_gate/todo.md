# R56 Todo

- define one bounded append-only trace-VM instruction set and state surface;
- define one transparent reference semantics implementation for that contract;
- execute the candidate internal trace-VM route free-running on the same rows;
- export full step traces, final states, and first-fail localization;
- reject teacher-forced or answer-only success as insufficient; and
- stop the mechanism lane immediately if step-trace exactness fails.
