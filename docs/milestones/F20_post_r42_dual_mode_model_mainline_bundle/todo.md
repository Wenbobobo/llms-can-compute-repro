# F20 Todo Closure

- make `coequal mainline` explicit without relabeling model-only success as
  exact execution evidence;
- record both admitted model implementations:
  `compiled_weight_executor` and `trainable_2d_executor`;
- keep `R43` explicit as the decisive next exact gate and `R45` explicit as a
  coequal comparator/operator lane;
- keep `R44` non-active until later `H42_post_r43_route_selection_packet`;
- keep `R41`, `F11`, `R29`, and `F3` non-active here.
