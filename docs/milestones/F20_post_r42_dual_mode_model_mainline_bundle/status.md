# F20 Status

- completed planning-only dual-mode model mainline bundle after `R42`;
- preserves `H40` as the prior semantic-boundary activation packet;
- preserves `H36` as the active routing/refreeze packet underneath the stack;
- records `coequal_mainline_exact_non_substitutive` as the model posture;
- records `compiled_weight_executor` and `trainable_2d_executor` as the two
  admitted model implementations;
- keeps exact `R43` decisive for the scientific route;
- keeps `R45` downstream of the exact contract surface and keeps `R44`
  deferred behind later `H42`.
