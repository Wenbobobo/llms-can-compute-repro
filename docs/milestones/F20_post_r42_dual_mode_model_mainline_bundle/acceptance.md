# F20 Acceptance

- the bundle remains planning-only and does not itself authorize execution;
- the coequal-mainline posture is explicit, but exact evidence remains
  scientifically decisive;
- both admitted model implementations are fixed by name and role;
- `R45` is downstream of the exact `R43` contract surface rather than ahead of
  it;
- `R44` remains deferred until a later post-`R43` route-selection packet;
- arbitrary `C`, unrestricted Wasm, and general-computer rhetoric remain
  excluded.
