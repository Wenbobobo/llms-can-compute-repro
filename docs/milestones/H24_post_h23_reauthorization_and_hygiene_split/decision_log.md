# Decision Log

- Keep `H23` as the frozen scientific state instead of treating `P14` as a new
  evidence lane.
- Split boundary reauthorization (`R30`) from systems reauthorization (`R31`)
  so one unresolved question cannot silently drag the other forward.
- Preserve `R29` and `F3` as blocked future lanes.
- Keep `F4` and `P15` subordinate to the decision packet; internal docs may be
  refreshed, but they do not authorize science or public-surface widening.
