# Acceptance

- `H23` is still named as the frozen scientific state.
- `P14` is still named as the completed downstream docs-only lane.
- the next packet order is explicit:
  `H24 -> R30 + R31 -> H25`.
- `R29` and `F3` remain blocked.
- no wider endpoint, broader compiled claim, or frontier/demo wording is
  introduced.
