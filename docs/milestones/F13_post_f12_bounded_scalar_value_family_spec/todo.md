# Todo

- [x] Choose exactly one richer same-substrate family downstream of `F10`.
- [x] Define the bounded semantics of that family.
- [x] Define how that family remains observable in append-only trace or final
  state without hidden semantics.
- [x] Define the lowering invariants and comparator set a later packet would
  need.
- [x] Define one bounded contrastive probe and explicit stop conditions.
