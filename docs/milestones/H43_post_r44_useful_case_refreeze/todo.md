# H43 Todo

- compare `freeze_r44_as_narrow_supported_here` against
  `continue_to_broader_semantic_boundary_execution_immediately` and
  `reopen_deferred_runtime_or_merge_routes_after_r44`;
- keep `H42`, `H36`, completed exact `R43`, completed useful-case `R44`,
  completed coequal `R45`, `F18`, `F19`, `F20`, and `P27` explicit inside the
  packet;
- update claim `D` in the long-arc claim ladder from `not_yet_validated_here`
  to `supported_here_narrowly`;
- return the stack to `no_active_downstream_runtime_lane` instead of
  inventing a broader runtime successor; and
- keep `R41`, `F11`, `R29`, `F3`, arbitrary `C`, unrestricted Wasm, and
  general “LLMs are computers” rhetoric non-active here.
