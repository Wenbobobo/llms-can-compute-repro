# TODO

- keep `wip/p56-main-scratch` as the only clean local integration base
- keep `wip/p66-post-p65-published-successor-freeze` as the live published
  clean descendant
- keep the read-only `git merge-tree` probe conflict-free and non-executing
- keep dirty root `main` out of bounds for merge-prep
