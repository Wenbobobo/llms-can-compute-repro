# R55 Acceptance

- `R55` remains an exact gate, not a larger demo;
- the task matrix stays fixed and bounded;
- reference and fast-path retrieval operate on the same read contract;
- exact value parity and exact maximizer-row parity are both required;
- declared tie handling is explicit rather than implicit;
- a negative result is first-class closeout evidence; and
- `R56` does not open unless `R55` is exact.
