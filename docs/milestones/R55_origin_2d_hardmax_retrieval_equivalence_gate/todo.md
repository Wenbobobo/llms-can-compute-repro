# R55 Todo

- define one transparent reference latest-relevant-state lookup;
- define one claimed `2D` hard-max retrieval path on the same query contract;
- fix one bounded task matrix covering overwrite-after-gap, stack-slot,
  duplicate-max, coordinate-offset, and tie cases;
- export exact value parity and maximizer-row parity on every read;
- localize the first failure explicitly if equivalence breaks; and
- stop the mechanism lane immediately if exact equivalence fails.
