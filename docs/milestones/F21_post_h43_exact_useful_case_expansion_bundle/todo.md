# F21 Todo Closure

- make exact-first post-`H43` reentry explicit without reopening runtime by
  momentum;
- define one surface-extension matrix for `R46`, one frontend-boundary matrix
  for `R47`, and one comparator scoreboard for `R48`;
- keep `H43` explicit as the preserved prior useful-case refreeze packet and
  current paper-grade endpoint until exact widening survives;
- keep `R47` and `R48` conditional downstream lanes rather than immediate
  runtime follow-ons; and
- keep `R41`, `F11`, `R29`, `F3`, broader Wasm/C, and merge-to-`main`
  non-active here.
