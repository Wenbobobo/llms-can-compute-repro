# TODO

- keep archive and release surfaces synchronized with the paper/submission
  package
- keep `archive_or_hygiene_stop` as the default downstream lane
- keep dirty-root `main` out of any outward handoff
