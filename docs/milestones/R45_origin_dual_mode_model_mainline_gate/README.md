# R45 Origin Dual-Mode Model Mainline Gate

Saved future coequal model gate after `F20/H41`.

`R45` is the model-mainline comparator/operator lane for the bounded-memory
post-`R42` route. It is intended to compare:

- `compiled_weight_executor`;
- `trainable_2d_executor`;
- exact `R43` contracts once the exact bounded-memory gate is fixed.
