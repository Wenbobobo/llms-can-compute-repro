# R45 Acceptance

- both admitted model modes are evaluated on the same bounded contract family;
- both admitted model modes are evaluated against the exact `R43` baseline;
- the fitted trainable mode reports its train/held-out family split explicitly;
- model-only positives do not replace exact `R43`;
- model-only failures do not invalidate exact `R43`; and
- `R45` stops below later route-selection and below any `R44` useful-case
  execution wave.
