# Baseline Contract

Any later route must beat or honestly lose against all of:

- a compiled linear-scan exact hard-max baseline;
- the current exact-route reference path; and
- an external simple or scalar baseline.

No Python-only baseline win counts as route evidence.
