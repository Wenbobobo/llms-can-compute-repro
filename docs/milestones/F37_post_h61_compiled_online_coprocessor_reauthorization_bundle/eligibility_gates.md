# Eligibility Gates

Before any runtime profile gate is even proposed, the route must specify:

- the exact lifted useful-kernel decode target;
- the retrieval-head cost share that is supposed to matter;
- the expected query:insert ratio;
- tie frequency and tie-handling burden; and
- why this cost structure is materially different from `R62/H58`.
