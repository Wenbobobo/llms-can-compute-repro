# P20 Post-H34 Manuscript Narrative Resync

Docs-only paper-facing resync after the landed `H32 -> H33 -> R39 -> H34`
chain.

`P20` exists to rewrite the manuscript-sized prose bundle and its immediate
helper docs so the current paper endpoint is the narrow Origin-core compiled
line rather than preserved post-`H21` same-endpoint placeholders.

This lane does not change the active scientific stage. It aligns paper-facing
wording to the already landed control state.
