# P20 Status

- completed docs-only manuscript and helper-doc resync after `H34`;
- makes `manuscript_bundle_draft.md` terminate on
  `R37 -> H30 -> H31 -> R38 -> H32 -> H33 -> R39 -> H34`;
- demotes preserved `R19/R20/R21/R22/R23/H21/H27` same-endpoint material to
  explicit historical context rather than current paper-assembly horizon;
- keeps `H32` as active routing, `H34` as the current docs-only control packet,
  and no active downstream runtime lane after `H34`.
