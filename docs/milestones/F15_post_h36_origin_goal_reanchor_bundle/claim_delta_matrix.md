# Claim Delta Matrix

| Origin-facing claim | Current status | Why |
| --- | --- | --- |
| Append-only execution traces can encode deterministic computation | `supported_here` | `H28 -> R34/R35 -> H29` remains the active mechanistic floor. |
| Exact `2D` hard-max retrieval can implement the key bounded read primitives | `supported_here` | `R34` still preserves the active narrow retrieval contract. |
| Latest-write / stack-like retrieval can support a small exact executor | `supported_here` | `R35` remains positive on the current narrow execution bundle. |
| One tiny compiled slice plus one richer same-opcode control/call family can target the current substrate exactly | `supported_here` | `R37/R38/R39/H32/H34` preserve that narrow compiled line. |
| One bounded same-substrate richer value family beyond the prior executor floor can target the current substrate exactly | `supported_here` | `H35 -> R40 -> H36` validates bounded scalar locals plus typed `FLAG` slots narrowly on the same substrate. |
| The next scientifically honest step is a contradiction-led runtime-relevance decision on the fixed landed `R40` row pair | `planning_only` | `H37` keeps the decision packet docs-only and still defaults to `keep_h36_freeze`. |
| The compiler or helper annotations may still be doing too much of the real work | `planning_only_contradiction_risk` | This remains the strongest same-substrate caution after `R40/H36`. |
| The fast path may only help the easy part of the bounded-scalar result | `planning_only_contradiction_risk` | This remains the nearest alternative explanation of the current narrow positive line. |
| Restricted Wasm-like or richer semantic-boundary work is already justified | `blocked_by_scope` | `F9` remains blocked even after the bounded scalar family landed. |
| Arbitrary `C` has been reproduced | `blocked_by_scope` | The active line remains narrow and same-substrate only. |
| A general LLM has become a computer | `blocked_by_scope` | The repo still rejects the headline framing as the current claim. |
| Broad systems/platform superiority has been established | `requires_new_substrate` | The current same-substrate line is too narrow and still not a broad systems result. |
