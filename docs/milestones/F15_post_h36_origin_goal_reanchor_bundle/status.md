# F15 Status

- completed planning-only origin-facing reanchor bundle after `H36/P24`;
- replaces `F12` as the current canonical derivative claim-delta surface;
- records bounded scalar locals and typed flags as `supported_here` narrowly;
- ranks the two surviving `F14` runtime-relevance cautions as the top
  unresolved same-substrate scientific gaps;
- does not authorize `R41` by itself.
