# F15 Post-H36 Origin Goal Reanchor Bundle

Planning-only origin-facing reanchor bundle after the landed `H36/P24` state
and the completed `P25` promotion-prep lane.

`F15` replaces `F12` as the current canonical derivative surface for mapping
the origin article and the three discussion files onto the current
`H36/P24/P25/H37` reading. It does not authorize runtime execution, scope
lift, or public-surface expansion.

This bundle preserves:

- `H37` as the current docs-only runtime-relevance decision packet;
- `H36` as the preserved prior active routing/refreeze packet;
- `P25` as the completed operational promotion-prep lane;
- `no active downstream runtime lane`.
