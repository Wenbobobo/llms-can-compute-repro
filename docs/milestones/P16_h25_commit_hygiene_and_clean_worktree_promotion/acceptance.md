# Acceptance

- one clean-worktree closeout path is explicit before any new runtime batch;
- any commit split remains path-scoped and reviewable;
- one explicit runbook exists for opening and validating the clean worktree;
- state-dependent hygiene/preflight outputs are regenerated in the commit
  worktree;
- `H25 active / H23 frozen` semantics remain unchanged;
- `P16` does not become a substitute science lane.
