# Todo

- [ ] Save the post-unattended next-stage design and refreshed active-wave
      handoff before opening the clean worktree.
- [x] Fix one commit split manifest before execution.
- [x] Fix one clean-worktree runbook before execution.
- [x] Open one clean worktree scaffold for the closeout lane.
- [ ] Isolate the current closeout into a reviewable clean-worktree subset.
- [ ] Split the closeout into `post-h23-core-packet` and
      `h25-closeout-audits` if one combined commit is not self-contained.
- [ ] Regenerate `release_worktree_hygiene_snapshot` and
      `release_preflight_checklist_audit` inside the clean worktree.
- [ ] Re-run the focused exporter tests for the closeout subset.
- [ ] Avoid sweeping unrelated dirty-tree paths into the same review.
- [ ] Finish this lane before opening `R32`.
