# P16 H25 Commit Hygiene And Clean Worktree Promotion

Immediate operational closeout lane after landed `H25`.

`P16` exists to isolate the current dirty-tree closeout into a clean,
reviewable worktree before any new runtime execution starts. It is not a new
science lane and it does not change the `H25 active / H23 frozen` scientific
reading.

Execution details are now fixed in:

- `commit_split_manifest.md`
- `worktree_runbook.md`
