# Trigger Matrix

| Candidate trigger class | Admissible same-substrate reopen trigger? | Current state after `H34` | Why it does not currently survive |
| --- | --- | --- | --- |
| Exactness or final-state contradiction on the admitted row or named same-family boundary probe under the current opcode surface | yes | not observed | `R37`, `R38`, and `R39` all preserve exact source/lowered/free-running execution on their declared rows |
| One sharper control-surface dependence on the same admitted rows than the declared `R39` helper-body permutation | yes | unresolved in principle, but not isolated concretely | `R39` weakened the strongest current version of this objection, and no single sharper same-substrate perturbation now dominates strongly enough to justify a reopen by itself |
| Evidence that the compiler/helper annotations are doing the substantive work while the runtime contribution disappears on the current rows | yes | concern preserved, contradiction not isolated | this remains a standing scientific risk, but the current bundle does not identify one new decisive comparator or failure mode beyond the already audited `R39` objection |
| Need to test a new compiled family, broader opcode surface, Wasm-like support, or arbitrary `C` | no | blocked by scope | this is scope lift, not a same-substrate contradiction inside the current admitted boundary |
| Historical same-endpoint systems dissatisfaction or lack of competitiveness | no | preserved historical negative context only | `H27` closes that route negatively and `H34` leaves no active downstream runtime lane on the current Origin-core line |
| Frontier/demo pressure or origin-headline mismatch | no | blocked by routing | these are planning or communication concerns, not contradiction packets against the current same-substrate evidence |

## Conclusion

Under the current evidence bundle, no admissible trigger class contains one
single sharpened candidate that survives strongly enough to justify a new
explicit reopen packet.
