# Candidate Ranking

## Ranked candidates

1. Sharper same-row control-surface dependence than the declared `R39`
   permutation
   - status: rejected for now
   - why: this is the strongest admissible reopen story in principle, but no
     unique perturbation currently dominates. Reopening without that sharper
     target would turn the line into exploratory momentum rather than
     contradiction-driven science.
2. Compiler/helper-body over-encoding on the admitted rows
   - status: rejected for now
   - why: still a valid threat model, but the current bundle lacks one new
     decisive comparator that would isolate runtime irrelevance more sharply
     than the already completed `R39` audit.
3. Numeric or retrieval fragility inside the current compiled rows
   - status: rejected for now
   - why: `R36` already bounds the precision story on the current broader
     Origin-core suite, and no compiled-row contradiction has emerged on the
     admitted or named same-family audit rows.
4. Broader family or language coverage dissatisfaction
   - status: inadmissible
   - why: this is scope lift, not a contradiction against the current narrow
     same-substrate packet.
5. Historical same-endpoint systems dissatisfaction
   - status: inadmissible
   - why: this belongs to the preserved `H21/H27` same-endpoint route, not the
     active Origin-core compiled line.

## Final verdict

`no_reopen_candidate_survives`

The current bundle preserves several reasons to stay scientifically cautious,
but none of them isolate exactly one same-substrate contradiction sharply
enough to justify a future reopen packet today.
