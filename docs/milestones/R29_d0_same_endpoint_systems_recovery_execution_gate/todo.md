# Todo

- [x] Read `H23` and confirm that no bounded same-endpoint recovery path was
  localized strongly enough to reopen this lane.
- [x] Record that any future execution contract must still be explicit,
  comparator-honest, and justified by new same-endpoint evidence.
- [x] Keep this lane blocked while `systems_still_mixed` and
  `bounded_grid_still_not_localized` remain unchanged.
