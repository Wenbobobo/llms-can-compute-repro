# Status

Blocked after `H23`.

- this lane must not activate while `systems_still_mixed` remains unchanged and
  unsupported by a new bounded recovery path;
- `H23` ended with `boundary_verdict = bounded_grid_still_not_localized`, so no
  scientifically clean same-endpoint recovery route was localized here;
- it exists only to store enough structure for a later explicit reopen plan;
- it must stay same-endpoint, comparator-honest, and bounded if later opened.
