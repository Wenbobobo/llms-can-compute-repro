# H31 Post-H30 Later Explicit Boundary Decision Packet

Executed docs-only later explicit packet after `H30` and the completed clean
closeout in `P17`.

`H31` answers the only allowed post-`H30` question on the current Origin-core
substrate: whether one richer compiled control/call family can be tested
without widening the opcode surface, changing retrieval/runtime semantics, or
softening blocked-lane discipline.

This packet chooses `execute_one_more_tiny_extension` only for:

- admitted row:
  `subroutine_braid_program(6, base_address=80)`;
- non-admission boundary row:
  `subroutine_braid_long_program(12, base_address=160)`.
