# H31 Todo

- lock one explicit post-`H30` outcome rather than leaving the fork implicit;
- fix the admitted row to `subroutine_braid_program(6, base_address=80)`;
- fix the boundary row to `subroutine_braid_long_program(12, base_address=160)`;
- keep `R29`, `F3`, and broad compiler scope blocked;
- route the only authorized next runtime lane to
  `R38_origin_compiler_control_surface_extension_gate`.
