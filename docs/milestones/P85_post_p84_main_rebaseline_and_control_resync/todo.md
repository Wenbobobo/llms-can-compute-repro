# TODO

- inventory dirty root `main` and classify tracked/untracked payloads before
  any root replacement;
- contract the mounted keep set after root archive/replace is safe; and
- refresh paper-facing live docs from the older `H43` narrative to the current
  `H65 + P85` archive-first closeout state.
