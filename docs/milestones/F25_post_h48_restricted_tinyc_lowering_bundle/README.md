# F25 Post-H48 Restricted Tiny-C Lowering Bundle

Placeholder only.

If `H48` selects the positive route after `R49`, `F25` will become the next
planning-only bundle for one restricted tiny-`C` lowering question on the
preserved useful-case contract.

Current status: `not_authorized_yet`.
