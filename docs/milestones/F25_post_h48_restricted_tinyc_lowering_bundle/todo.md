# F25 Todo

- save the dedicated `F25` design;
- define one admitted restricted tiny-`C` surface matrix on top of the
  preserved useful-case kernels;
- define one explicit excluded-feature matrix before execution;
- define one execution matrix that starts on the preserved `R47` `8/8`
  useful-case variants across the fixed `3/3` kernel ladder;
- define one explicit kill-criteria sheet for early stop; and
- keep numeric widening, heap, recursion, floats, IO, alias-heavy pointers,
  library calls, and arbitrary `C` out of scope unless a later explicit packet
  changes the boundary.
