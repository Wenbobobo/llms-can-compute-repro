# Downstream Gate

`R63` is not a runtime reopen.

If later authorized, it may only profile feasibility of the compiled-online
coprocessor route under the `F37` contract. Failure at that gate should return
to archive/hygiene stop, not widen scope.
