# 2026-03-31 Post-P62 Tight-Core Hygiene And Gate-Dossier Design

## Recommended Main Route

The recommended post-`P62` route is:

`tight-core hygiene -> docs router + taxonomy cleanup -> F38 gate-dossier-only closeout`

This keeps the live branch posture honest, reduces worktree sprawl, and leaves
future science in a dormant admissibility dossier rather than reopening
runtime, same-lane executor-value work, or broad substrate expansion.

## Locked Facts

- `H64_post_p53_p54_p55_f38_archive_first_freeze_packet` remains the active
  docs-only packet
- `P56/P57/P58/P59` remain the landed follow-through foundation
- `P60/P61/P62` remain the current published clean-descendant stack
- `wip/p60-post-p59-published-clean-descendant-prep` remains the published
  clean descendant
- `wip/p56-main-scratch` remains preserved local integration ancestry only
- dirty root `main` remains quarantine-only
- merge posture remains `clean_descendant_only_never_dirty_root_main`
- runtime remains closed
- `F38/R63` remains dormant and non-runtime only
- the default downstream lane remains `archive_or_hygiene_stop`

## Waves

### Wave 1

`tight_core_inventory_and_keep_set_lock`

- add a branch/worktree registry that separates the live keep set from archive
  and cleanup candidates
- keep `p60`, `p63`, `p56-main-scratch`, and dirty-root quarantine explicit
- identify `p16/r32/r33` as a redundant local-only ancestor chain

### Wave 2

`clean_descendant_merge_topology_rebaseline`

- keep `p60` as the published clean source
- keep `p63` as the mutable hygiene follow-through branch only
- record that dirty root `main` remains out of scope for integration

### Wave 3

`docs_router_and_taxonomy_cleanup`

- add `docs/README.md` as a live router
- tighten `README.md`
- reframe `docs/plans/README.md`, `docs/milestones/README.md`, and
  `docs/publication_record/README.md` around live vs historical vs dormant
  reading order

### Wave 4

`f38_gate_dossier_only_closeout`

- keep `R63` dormant and non-runtime only
- require an exact useful target, honest comparator, cost share,
  query:insert ratio, tie burden, and a material cost delta versus `R62/H58`
  before any later non-runtime eligibility reassessment is even discussable
- do not reopen runtime, broad Wasm, arbitrary `C`, or transformed/trainable
  entry planning here

### Wave 5

`next_planmode_surface_refresh`

- refresh the current brief prompt, startup prompt, and handoff in place
- preserve `H64 + P56/P57/P58/P59 + P60/P61/P62 + F38` as the locked live
  starting point for the next planning round
- keep the next route centered on hygiene, merge-prep, archive polish, or
  explicit stop
