Current locked facts:

- active closeout wave:
  `P84_post_p83_keep_set_contraction_and_closeout`
- live promotion-ready branch:
  `wip/p83-post-p82-promotion-branch-and-pr-handoff`
- published branch head:
  `53962ca`
- release-preflight state:
  `docs_and_audits_green`
- submission/archive state:
  `archive_ready`
- recommended route:
  promotion/PR finalization
- fallback:
  no further action
- future screen:
  runtime closed; `R63` remains strictly non-runtime only

In plan mode:

1. Recommend one main next route first.
2. Keep runtime closed and avoid dirty-root integration or same-lane reopen.
