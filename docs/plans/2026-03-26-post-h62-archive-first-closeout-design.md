# 2026-03-26 Post-H62 Archive-First Closeout Design

## Purpose

Turn the post-`H62` archive-first posture into one explicit closeout wave that:

- makes archive / hygiene / merge-prep / paper-facing partial-falsification the
  current active route;
- keeps `R63` only as a dormant non-runtime dossier and not as authorization;
- preserves the narrow mechanistic positive endpoint while refusing same-lane
  executor-value reopen; and
- keeps dirty root `main` fully outside the integration base.

## Selected Wave

This wave lands five packets:

1. `P50_post_h62_archive_first_control_sync`
2. `P51_post_h62_paper_facing_partial_falsification_package`
3. `P52_post_h62_clean_descendant_hygiene_and_merge_prep`
4. `F38_post_h62_r63_dormant_eligibility_profile_dossier`
5. `H63_post_p50_p51_p52_f38_archive_first_closeout_packet`

## Hard Constraints

- no runtime gate opens in this wave;
- no same-lane executor-value microvariant is revived;
- `R63` remains non-runtime and dormant;
- transformed/trainable entry stays blocked through `F27/R53/R54`;
- no broad Wasm or arbitrary `C` widening is planned;
- dirty root `main` remains quarantine-only; and
- GPTPro / Origin material remains advisory-only.

## Wave Outputs

### P50 control sync

- refresh root and control surfaces so they all say the same thing:
  `H63` active, `H62` preserved prior, archive-first closeout current, runtime
  closed, `R63` dormant only.

### P51 paper-facing package

- refresh paper, review, submission, and archive-facing wording to the strongest
  honest framing:
  narrow mechanistic reproduction survives, broad headline reproduction did not
  land, and the strongest justified executor-value lane is closed negative.

### P52 hygiene and merge-prep

- refresh clean-descendant-only merge-prep notes, large-artifact policy, branch
  map, and root quarantine wording without executing any merge.

### F38 dormant R63 dossier

- record the only surviving future family as a dormant no-go dossier under the
  `F37` contract;
- keep the exact useful target explicit; and
- keep retrieval-head cost share, query:insert ratio, tie burden, and material
  cost-model delta unresolved rather than guessed.

### H63 closeout packet

- make the new repo meaning machine-readable:
  archive-first closeout is the active route and `R63` stays dormant.

## Required Outcome

After this wave the repo should say, machine-readably:

- `H63` is the current active docs-only packet;
- `H62` remains the preserved prior active packet;
- `P50`, `P51`, and `P52` are the active closeout sidecars;
- `F38` is the current dormant future dossier;
- `archive_or_hygiene_stop` remains the default downstream lane;
- `r63_post_h62_coprocessor_eligibility_profile_gate` remains conditional and
  non-runtime only; and
- any future scientific continuation still requires a later explicit packet and
  must beat archive-first stop honestly.
