# 2026-03-26 Post-H63 Next Plan-Mode Startup Prompt

Use the block below as the next planning-round entry prompt after the `H63`
archive-first closeout phase and the standing-audit refresh.

```text
Let us continue this reproduction-oriented research engineering project in plan mode.

Current locked facts:
- The current active docs-only packet is `H63_post_p50_p51_p52_f38_archive_first_closeout_packet`.
- The preserved prior active packet is `H62_post_p47_p48_p49_f37_hygiene_first_scope_decision_packet`.
- The current control sync wave is `P50_post_h62_archive_first_control_sync`.
- The current paper-facing package wave is `P51_post_h62_paper_facing_partial_falsification_package`.
- The current repo-hygiene sidecar is `P52_post_h62_clean_descendant_hygiene_and_merge_prep`.
- The current dormant future dossier is `F38_post_h62_r63_dormant_eligibility_profile_dossier`.
- The default downstream lane is `archive_or_hygiene_stop`.
- The only conditional later gate is `r63_post_h62_coprocessor_eligibility_profile_gate`.
- Runtime remains closed.
- No same-lane executor-value microvariant is admissible.
- The strongest justified executor-value lane is already closed negative at preserved `H58_post_r62_origin_value_boundary_closeout_packet`.
- The preserved paper-grade endpoint remains `H43_post_r44_useful_case_refreeze`.
- Dirty root `main` remains quarantine-only and must not be used as an integration base.
- The clean working branch is `wip/f38-post-h62-archive-first-closeout` in worktree `D:/zWenbo/AI/wt/f38-post-h62-archive-first-closeout`.
- Standing `release_preflight_checklist_audit` and `P10_submission_archive_ready` have been refreshed to the `H63` state and are green.
- GPTPro / Origin materials remain advisory only, not evidence.

Hard constraints:
- Do not reopen same-lane executor-value probe variants.
- Do not treat `R63` as runtime authorization.
- Do not plan transformed/trainable entry, broad Wasm, or arbitrary `C`.
- Do not route any merge through dirty root `main`.
- Do not treat advisory documents as evidence.

Please do the following in plan mode:
1. First decide whether the project has already reached its strongest honest stopping point under `H63`, and whether the next phase should be archive / hygiene / promotion-prep / merge-prep / paper-facing packaging only.
2. If any scientific continuation is still worth discussing, keep it strictly inside the dormant non-runtime `R63` family and argue explicitly:
   - the exact useful target;
   - the missing cost-share fields;
   - query:insert ratio;
   - tie burden;
   - materially different cost-model delta;
   - and why that route would differ substantively from the already closed `H58/H62` state.
3. Produce one recommended main route first. If you discuss alternatives, rank them clearly and explain why the recommended route dominates.
4. Write the next-phase plan as explicit waves. For each wave include:
   - objective;
   - required inputs;
   - expected outputs;
   - stop conditions;
   - go/no-go rule;
   - expected commits;
   - whether a new worktree or subagent is needed.
5. Before execution, save the planning document to `docs/plans/YYYY-MM-DD-post-h63-<topic>-design.md`.
6. Prefer clean-descendant-only publication, hygiene, promotion-prep, and merge-prep work over speculative science.

Recommended planning bias:
- Start from `H63` as the current truthful base state, not from older reopen momentum.
- Be willing to conclude that the correct next phase is archive-first closeout, hygiene, promotion-prep, or partial-falsification packaging only.
- Only keep `R63` alive if the plan can honestly name concrete missing fields and a non-runtime path to resolve them.
- If no such path exists, propose a stop / archive-first closeout / promotion-prep plan rather than stretching the science lane.
```
