# 2026-03-26 Post-H60 Next Plan-Mode Handoff

## Purpose

This note is the shortest safe entrypoint for the next plan-mode pass after the
archive-first consolidation wave landed on a clean successor worktree.

Use it to avoid reconstructing the current repo meaning from older reopen-era
or execution-era waves.

## Current Locked State

The current active docs-only packet is
`H61_post_h60_archive_first_position_packet`.

The current repo meaning is:

- narrow mechanistic reproduction survives;
- broader headline reproduction did not land;
- the strongest justified executor-value lane is closed negative enough to
  treat same-lane reopen as inadmissible;
- archive-first consolidation is now the default live posture;
- the only still-admissible future family on paper is
  `compiled_online_exact_retrieval_primitive_or_attention_coprocessor_route`;
- that family is qualification-only at `F36` and not runtime-authorized; and
- the current downstream lane remains `planning_only_or_project_stop`.

## What Just Landed

The clean worktree
`D:/zWenbo/AI/wt/f36-post-h60-archive-first-consolidation`
now contains the complete post-`H60` archive-first control wave:

- `P45_post_h60_clean_descendant_integration_readiness`
- `F36_post_h60_conditional_compiled_online_reopen_qualification_bundle`
- `H61_post_h60_archive_first_position_packet`
- `P46_post_h60_archive_first_publication_sync`

Validation state at handoff time:

- four new exporters ran successfully under `uv`;
- focused exporter tests passed: `4 passed`;
- `git diff --check` was clean;
- `P46` locked all `11/11` audited files with `blocked_count = 0`; and
- the branch is currently committed clean at `ead6652` on top of `542cfe2`.

## Branch And Merge Posture

- current branch: `wip/f36-post-h60-archive-first-consolidation`
- current worktree:
  `D:/zWenbo/AI/wt/f36-post-h60-archive-first-consolidation`
- this branch now tracks
  `origin/wip/f36-post-h60-archive-first-consolidation`
- dirty root repo remains parked separately as
  `wip/root-main-parking-2026-03-24`
- the parked root checkout currently shows about `120` dirty/untracked entries
  and should be treated as quarantine-only rather than as an integration base
- root repo `main` is not the integration target for scientific work
- merge posture remains `clean_descendant_only_never_dirty_root_main`
- no merge back to dirty root `main` was executed during this wave

If a later hygiene/promotion pass happens, it should route only between clean
descendants and should continue to treat dirty root `main` as quarantined.

## Hard Constraints For The Next Plan

Do not plan any of the following as the next execution wave:

- another `R62`-like executor-value microvariant on the same cost structure;
- transformed/trainable entry through `F27`, `R53`, or `R54`;
- broad Wasm or arbitrary `C` scope lift;
- any plan that treats `F36` as runtime authorization by itself;
- any merge-back plan that assumes dirty root `main` becomes the clean source
  of truth; or
- any plan that treats GPTPro advisory material as evidence.

If a future wave is proposed, it must first beat the stop/archive option and
must answer the `F36` qualification questions before any runtime packet is
authorized.

## Recommended Next Plan-Mode Questions

### 1. Archive-first stop versus one more explicit authorization screen

The next planner should first decide whether the project has already reached
its strongest honest endpoint:

- narrow positive mechanistic reproduction;
- broader headline negative; and
- executor-value locally falsified on the strongest justified lane.

If yes, the correct next phase is archive polish, repo hygiene, branch
normalization, and paper-facing partial-falsification packaging rather than a
new execution wave.

### 2. If one future route still survives, can it clear `F36` concretely?

If the planner believes one future route remains scientifically credible, it
must stay inside `F36` and answer these before authorizing runtime:

- what exact useful-case target remains alive?
- what exact cost-model change is materially different from `R62/H58`?
- what transparent linear and external/simple baselines must it beat?
- what immediate stop rules kill the lane if it collapses into renamed
  same-lane work?
- why is the proposal qualification-worthy rather than just another wording
  drift?

The bar should remain high. A vague "compiled-online attention might help"
story is still not enough.

### 3. Dedicated hygiene and integration preparation phase

If science remains planning-only, the next plan can justify a dedicated hygiene
phase:

- branch graph and worktree normalization;
- clean-descendant merge/promotion sequencing;
- archive-surface slimming and large-artifact policy checks; and
- optional merge planning only among clean descendants, never through dirty
  root `main`.

## Recommended Reading Order

Read these first:

1. `docs/publication_record/current_stage_driver.md`
2. `tmp/active_wave_plan.md`
3. `docs/plans/2026-03-26-post-h60-archive-first-consolidation-design.md`
4. `results/H61_post_h60_archive_first_position_packet/summary.json`
5. `results/F36_post_h60_conditional_compiled_online_reopen_qualification_bundle/summary.json`
6. `results/P46_post_h60_archive_first_publication_sync/summary.json`
7. `results/P45_post_h60_clean_descendant_integration_readiness/summary.json`
8. `results/F35_post_h59_far_future_model_and_weights_horizon_log/summary.json`
9. `results/H60_post_f34_next_lane_decision_packet/summary.json`

Read these after the active stack is clear:

- `docs/plans/2026-03-25-post-h59-gptpro-reinterview-dossier.md`
- `docs/Origin/QA1.md`
- `docs/Origin/QA2.md`

## Default Recommendation

Default recommendation for the next plan-mode pass:

- start from `H61`, not from older execution momentum;
- treat archive-first plus partial falsification as the current honest base
  state;
- prefer stop/archive/hygiene unless a materially different compiled-online
  route can be specified concretely under `F36`; and
- require a new explicit authorization packet before any runtime work resumes.
