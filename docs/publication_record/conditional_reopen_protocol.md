# Conditional Reopen Protocol

State: `archive_first_dormant_protocol`.

Only one future route remains admissible on paper:

- `compiled_online_exact_retrieval_primitive_or_attention_coprocessor_route`

Before any later runtime authorization is even discussable, the proposer must
provide a dormant dossier. This is now the `F38` standard. `R63` remains
dormant and non-runtime only until those fields are resolved honestly.
