# Conditional Reopen Protocol

State: `archive_first_dormant_protocol`.

Only one future route remains admissible on paper:

- `compiled_online_exact_retrieval_primitive_or_attention_coprocessor_route`

Before any later non-runtime eligibility reassessment is even discussable, the
proposer must provide a dormant dossier that names the exact useful target, the
honest comparator, cost share, query:insert ratio, tie burden, and a material
cost delta versus `R62/H58`. This is now the `F38` standard. `R63` remains
dormant and non-runtime only until those fields are resolved honestly. Runtime
authorization remains closed and is not what `R63` means.
