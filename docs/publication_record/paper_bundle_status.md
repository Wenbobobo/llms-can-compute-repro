# Paper Bundle Status

This file records the current paper-facing bundle state after the `P87`
refresh on top of `P86` and `P85`.

## Current Posture

The paper bundle is not widening toward a new runtime or executor-value claim.

Authoritative control now lives at the `H65 + P87 + P86 + P85` layer:

- `H65_post_p66_p67_p68_archive_first_terminal_freeze_packet` remains the
  active docs-only packet.
- `P87_post_p86_paper_spine_refresh_and_salvage_shortlist` is the current
  paper-spine refresh wave.
- `P86_post_p85_dirty_root_inventory_and_archive_replace_map` is the current
  dirty-root inventory wave.
- `P85_post_p84_main_rebaseline_and_control_resync` is the current clean
  merged-main rebaseline wave.
- `F38_post_h62_r63_dormant_eligibility_profile_dossier` remains the only
  dormant future dossier.

The correct paper-facing summary remains: narrow mechanistic reproduction
survives, the strongest useful-case executor-value lane closed negative, and
the repo is now in archive-first terminal-freeze follow-through rather than
scientific widening.

## Mandatory Paper-Facing Facts

- keep the project framed as a narrow execution-substrate reproduction, not a
  broad “LLMs are computers” thesis;
- keep `H58` as the binding value-negative closeout of the strongest admitted
  useful-case lane;
- keep `H59` as the preserved reproduction-gap packet above that closeout;
- keep `H65` as the live archive-first terminal-freeze packet for current
  outward wording;
- keep `P87` as the current paper-spine refresh wave and `P86` as the current
  dirty-root inventory wave;
- keep same-lane executor-value reopen closed; and
- keep `F38/R63` dormant and non-runtime rather than implying a reopened lane.

## Current Writing Boundary

Any paper-facing draft should preserve all of the following:

- append-only trace semantics and exact 2D hard-max retrieval remain the
  supported narrow positive mechanism;
- broad headline reproduction did not land;
- no claim of broad Wasm, arbitrary `C`, transformed/trainable entry, or
  same-endpoint practical speedup is justified; and
- any future continuation would require a later explicit packet with a
  materially different cost structure, not momentum from the closed lane.
